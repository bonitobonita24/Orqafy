# Project State — Orqafy
# Auto-generated by Claude Code. Never edit manually.
# Updated: 2026-07-11 by CLAUDE_CODE (RBAC §4 ROLLOUT Track C — 20/35 routers migrated, owner-paused for reboot)

PHASE:        RBAC §4 ROLLOUT (Track C router matrix migration), continuing M9. Owner said "yes do it"
              → migrated 15 more routers to the permission matrix this session (20/35 total). All LOCAL on
              feat/tenant-rbac-3tier, HARD HOLD (unpushed). Owner then asked to save + hand off + reboot
              the loop BEFORE the deferred routers. NOT terminal — Track C has 4 deferred routers (owner
              [WHAT]) + Tracks A/B not started. HEAD 1b7a604.
LAST_DONE:    RBAC §4 Track C — 15 routers migrated to matrixProcedure/writeProcedure.use(matrixMiddleware)
              in reviewed waves, each PM-ground-truth verified (web typecheck 0 · full web vitest green
              → 1218 tests · eslint clean · lint-design PASS) before commit:
              • 49d141c expense · e4e2469 report/employee/project/tasks/dtr · 0174790 invoice/client/
                support/job-order/pos · 77c82b1 crm/inventory/banking/payroll.
              • 8168a0d FIX — caught+fixed 2 real gating regressions INVISIBLE to tests (all matrix tests
                run under Platform-Owner bypass): dtr clockOut/leaveRequestCancel were →update (HR-only,
                locked out staff) → remapped to create; job-order removePart/removeServiceLine were
                →delete (bypass-only) → remapped to update. ⭐ PRINCIPLE: the ground-truthed seed
                packages/db/src/seed/role-permissions.ts is the AUTHORITATIVE endpoint→action map; naive
                CRUD-by-name diverges + regresses. Anti-regression rule in docs/RBAC_S4_ROLLOUT_PLAN.md.
              • ea02447 FIX design-stop-hook (was blocking every stop) — populated docs/STATE.md Rule 32
                evidence block with real gate output; no hook disabled.
              • 1b7a604 governance — rollout plan progress + deferred-router writeup.
              Deferred on owner [WHAT] (seed TODO(D-RBAC-DEADGATE) flags): dsr (RA10173 data-subject
              rights — reverted from matrix), accounting (broad-vs-accountant write mismatch), purchasing
              (approve dead-gate), storefront (requireAdmin TSA-exclusion). Track A (nav filter) + Track B
              (role-builder UI) NOT started.
              ── prior history ──
              M9 (0f3f811+5e20614) — RBAC §4 matrix INFRASTRUCTURE (role_permissions migration
              20260711000414 APPLIED to dev; hasPermission resolver, matrixMiddleware/matrixProcedure,
              guardPage, roleId in ctx; first 5 routers migrated). Verified green (web 1129/1129).
              M7 (P2 security + a11y hardening) finished end-to-end, all LOCAL, feat/tenant-rbac-3tier,
              HARD HOLD. 12 commits (75bc162..418a3c8). web typecheck 0 · web suite 1101/1101 (≈+35
              cross-tenant regression tests) · lint-design PASS · worker typecheck 0 · live app smoke
              PASS (login→dashboard→inventory→employees render, no 500s; guarded reads work). DEV STACK UP
              (app 42951, db 42941, redis 42943).
              • M7.1 (75bc162): StatutoryRate.upsert `config` z.record(z.unknown()) → typed 4-branch
                z.discriminatedUnion on `type` (sss/philhealth/pagibig/withholding), each .strict(),
                nonnegative. Last Zod input bypass closed (grep = 0 z.unknown()/z.any() on server inputs).
              • M7.4 (7d5ac4a): shadcn Skeleton installed; 10 app-shell loading.tsx spinner→Skeleton
                (dashboard stat-card/chart grammar; 9 table pages uniform grammar), ui-rules R11 PATH A,
                no twin files; login/loading.tsx left minimal.
              • M7.3 (audit, no code): storage upload posture STRONG (6/7 controls — MIME whitelist,
                SVG/HTML BLOCKED, attachment content-disposition, tenant-slug key prefix, randomized
                names, download tenant-verified). SSRF: NONE (only fixed-host Cloudflare siteverify).
                Residual: magic-byte sniff MISSING — architectural consequence of presigned direct-to-S3
                (server never sees bytes); XSS vector already closed by SVG/HTML block + forced-download
                → DOCUMENT-AND-ACCEPT (a bounded confirmUpload download-and-sniff is a possible future
                enhancement, judged disproportionate now).
              • M7.2 ESCALATED — the P2 "nested-include, low risk" note was WRONG. PM ground-truth
                verification + 3 read-only scouts (read-IDOR / list-leak / raw-FK-write) found REAL
                cross-tenant IDOR across 9 routers (M5 had removed the L6 auto-tenant-guard; every write
                must explicitly validate tenant). Fixed with the established
                `findFirst({id,tenantId})`/`loadXForTenant`/batch-count idiom + regression tests:
                - inventory (544c5d0 + 6eb4c0f): productUpdate SEVERE record-IDOR (findUnique→findFirst),
                  productById read-IDOR, productToggleActive, productList LIST-LEAK (no tenantId at all),
                  product/category/stock FK writes.
                - project (d9a4983): recordProjectExpense fundSourceId debit IDOR.
                - job-order (ff5f490): addPart productId + create customerId.
                - purchasing (0e0745d): goodsReceipt.list LIST-LEAK (ctx never destructured), PO create
                  nested item.productId/alloc.projectId/warehouseId, PO update vendorId, GR create productId.
                - pos (aa91261): sale warehouseId + customerId.
                - crm (564ebea): quotationCreate + quotationUpdate lineItems[].productId.
                - tasks+support (4c347a1): taskCreate parentTaskId, ticket.assign assignedToId, +bonus
                  taskAssign userId (unguarded — worse than flagged).
                - employee (306b558): create userId membership + create/update departmentId FK.
                - invoice/expense/department + defensive storefront (418a3c8): invoice create/update
                  customerId+projectId, expense create projectId, department update parentId, storefront
                  placeOrder explicit warehouse tenant-check.
              Scouts CLEARED as PROTECTED: banking, accounting, dtr, payroll, compliance, dsr, report,
              user, admin-xendit, smtp, notification (all FK writes validated / self-scoped / not
              tenant-relational). Read-IDOR: 0 outside inventory. List-leak: only the 1 goodsReceipt (fixed).
M8 (this session, no code change — verification only): closed the IDOR audit-coverage gap
              the prior session's own logged lesson ("write-focused-IDOR-audit-undercoverage") warned of.
              M7.2 had accounted for 24 routers (13 fixed + 11 scout-cleared) + 3 covered elsewhere (demo
              M3, storage M7.3, platform=intentionally cross-tenant). The remaining 6 (auth, client,
              expense-category, plan, registration, tenant) had NEVER been examined. Dispatched a read-only
              scout with the exact post-L6 threat model + guard idiom; ALL 6 = CLEAN. PM ground-truth
              spot-verified the two HIGH-priority write routers at the cited lines (client.ts list/byId/
              update/delete all tenant-scoped or findFirst({id,tenantId})-guarded; expense-category.ts
              update/delete membership-checked existing.tenantId===ctx.tenantId; tenant.ts keys off trusted
              ctx.tenantId only; auth/plan/registration = public/global, no tenant context). Full router
              surface 33/33 now verified. Working tree still CLEAN (no code change), HEAD dbe7bf0.

NEXT:         Owner PAUSED the rollout to hand off + reboot BEFORE the deferred routers. On resume:
              orient from THIS file + git (HEAD 1b7a604, tree clean), report Track C status, then ASK the
              owner which of these to do (they were about to choose A/B/C/D — see handoff). Full plan:
              **docs/RBAC_S4_ROLLOUT_PLAN.md**.
              (1) ⏸ 4 DEFERRED routers need owner [WHAT] answers (seed TODO(D-RBAC-DEADGATE) flags):
                  • dsr — self-service = RA10173 data-subject rights (reverted from matrix). Role-gate or not?
                  • accounting — mix of ungated-broad writes + accountant-only writes; seed grants
                    accounting-write to accountant-only → migrating broad writes NARROWS them. Gap to
                    tighten, or genuinely broad?
                  • purchasing — `approve` dead-gate (403 for everyone incl bypass). Fix or preserve?
                    (rest: reads + vendor/PO create/update + reactivate→delete are safely migratable now.)
                  • storefront — requireAdmin excludes Tenant Super Admin (dead "Administrator" name). Bug
                    to fix (widen to TSA) or preserve PO-only? (public + placeOrder + customer reads safe.)
              (2) Track A — sidebar nav filtering by matrix `view` (needs a me.permissions tRPC query).
              (3) Track B — tenant_superadmin-only role-builder UI (shadcn only; NEVER grant billing/users
                  to custom roles; guardrails in packages/db/src/rbac/guardrails.ts).
              ANTI-REGRESSION RULE (learned this session, non-negotiable): the seed
              packages/db/src/seed/role-permissions.ts is the AUTHORITATIVE endpoint→action map. Map an
              ungated/self-service write to a seed-TRUE action; only genuine primary-entity hard-deletes →
              delete (delete:true features only). Matrix tests run under Platform-Owner BYPASS, so they do
              NOT catch mis-gating — PM must diff each endpoint→action against the seed. Add a non-bypass
              allow/deny test per feature. Do NOT re-audit IDOR (33/33, M8) or re-run P1/P2 (M5/M6/M7).
              Login (dev): admin@mail.com / admin (workspace 'demo') · user@mail.com / user ·
              webmaster@orqafy.local (pw in CREDENTIALS.md). Tenant URL /demo/... App http://localhost:42951.
BLOCKERS:     none for [HOW]. Owner-gated remain HARD HOLD (see OWNER-GATED).
GIT_BRANCH:   feat/tenant-rbac-3tier (unpushed — HARD HOLD). HEAD 1b7a604 (RBAC §4 Track C 20/35;
              7 commits this session 49d141c..1b7a604; after v0.10.0 release tag @ d2d6196).
DEV STACK:    UP (db/cache/storage/mailhog/app_hot hot-reload). Host-side DB: build DATABASE_URL from
              DB_USER/DB_PASSWORD/localhost:42941/DB_NAME (.env.dev uses ${..} Prisma won't resolve).
              REDIS localhost:42943. Screenshots → screenshots/ (gitignored).
OWNER-GATED (docs/PENDING_DECISIONS.md): D-MIG-APPLY (apply dev migrations 20260710160000 +
              20260711010000 to staging/prod) · D-RBAC-C1..C4 (Wave C RBAC + reseed) · D-PRIV-1 (RA 10173)
              · framework-sync push · git-tag push (next release = MINOR 0.10.0) · staging/prod deploy ·
              D-1/3/4 product scope · NEW: D-NUM-1 (per-tenant document numbering — see NEXT (a)).
MODELS:
  planning:   claude-opus (PM/architect)
  execution:  spec-executor (Sonnet) workers
  governance: claude-code

# Project State — Orqafy
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-21 21:18 GMT+8 by CLAUDE_CODE Opus 4.7 (Batch 21a CLOSE — encryption infrastructure shipped, no consumers)

PHASE:        ✅ Phase 8 Batch 21a COMPLETE — Direction F prerequisite (split 1 of 3) SHIPPED. Pure foundational batch: encryption layer infrastructure with ZERO consumers. NEW apps/web/src/lib/crypto.ts (62 lines — AES-256-GCM via node:crypto, encrypt/decrypt, base64url format `{iv-12B}.{authTag-16B}.{ciphertext}`, lazy env read for testability, validates 32-byte key length + 12B IV + 16B auth tag) + 9 unit tests covering roundtrip / IV randomness / tampering rejection / malformed input / non-ASCII / missing-key / wrong-key-length. APP_ENCRYPTION_KEY added to env.ts serverSchema (REQUIRED, min(44) chars base64). .env.dev populated with dev key (gitignored). Squash-merged as 976df52. 706/706 GREEN (was 697, +9). 22→23 test files. Web typecheck + lint clean. ZERO schema changes (*Enc columns already existed in init migration). Rule 23 maintained — single discrete branch (feat/batch-21a-crypto-infrastructure), squash-merged + -D deleted.
HANDOFF:      none (clean close — Batch 21b queued: TenantXenditConfig CRUD router + admin UI + test-connection procedure. 21c after: refactor xendit.ts + webhooks/route.ts to use per-tenant config)
TOKEN_ESTIMATE: ~6K single Opus 4.7 session — pre-flight (with major scope-pivot via AskUserQuestion) + 9 RED tests at module-load time + crypto.ts helper + env.ts schema + .env.dev key + quality gates + commit + governance close. Extremely small batch — no /clear, no thrash, deep within 80K SAFE zone.
CHECKPOINT_TYPE: full
TIER_CLASSIFICATION: T1-T2 (4 actual files: 2 NEW + 2 MOD = far under 12-file threshold; 2 module surfaces — lib/crypto + env; no DB; security-critical primitive). Originally proposed 5 files but .env.example doesn't exist in this project (Bootstrap-template assumption, surfaced at pre-flight). RED→GREEN explicit: 9 tests written first, vitest confirmed file-level RED (Cannot find module '@/lib/crypto'), implemented helper → 9/9 GREEN on first run (no fix iteration).

FILES_TOUCHED (Batch 21a, squash-merged):
  - apps/web/src/lib/crypto.ts (NEW, 62 lines: AES-256-GCM encrypt/decrypt using node:crypto built-in; getKey() reads process.env.APP_ENCRYPTION_KEY lazily — base64-decodes, asserts 32-byte length; encrypt() generates 12B random IV per call, returns `iv.tag.ct` base64url; decrypt() validates IV length + auth tag length before decipher.setAuthTag — throws on tamper)
  - apps/web/src/__tests__/crypto.test.ts (NEW, 96 lines, 9 tests: roundtrip preserves plaintext, IV randomness across calls, tampered ciphertext throws, tampered auth tag throws, malformed input rejected with /malformed/, wrong IV length rejected with /iv/, non-ASCII roundtrip (emoji + Chinese + null bytes), missing APP_ENCRYPTION_KEY rejected on both encrypt+decrypt, wrong-length key rejected with /32 bytes/. beforeEach sets valid test key; afterAll restores original.)
  - apps/web/src/env.ts (+5 lines: APP_ENCRYPTION_KEY field on serverSchema as z.string().min(44) REQUIRED for production safety; matching entry in safeParse mapping)
  - .env.dev (+3 lines, gitignored: APP_ENCRYPTION_KEY=jDpC1ttbGNy6FCBblDpzZS+1/QoppWKfu2xaSjZ4s3Q= generated via openssl rand -base64 32, with comment about generation method)

BATCH_21a_PLAN (FULLY SHIPPED 2026-05-21):
  ✅ Item 1 (this batch): Foundational crypto primitive with no consumers
  ⬜ Batch 21b NEXT: TenantXenditConfig CRUD router (Tenant Administrator role) + admin UI page at /[slug]/(app)/settings/xendit + test-connection procedure ($0.01 invoice creation + void). Uses encrypt/decrypt from 21a. Estimated ~25K T2.
  ⬜ Batch 21c AFTER: Refactor getXenditClient(tenantId) to read TenantXenditConfig + decrypt; refactor webhook to identify tenant via xenditPaymentId lookup BEFORE signature verify (lookup → EcommerceOrder → tenantId → TenantXenditConfig.webhookTokenEnc → decrypt → timingSafeEqual). Estimated ~20K T2. Direction F COMPLETE.

BATCH_21a_PROCESS_NOTES:
  - vercel-plugin auto-suggestion hooks fired AGAIN on routine reads (next-forge on storefront.ts (from earlier session), bootstrap+vercel-storage on schema.prisma, env-vars on .env.dev). ALL correctly skipped per Rule 28 priority order + SessionStart instruction. Now 9th consecutive batch this pattern holds.
  - MAJOR PRE-FLIGHT SURPRISE: Direction F as scoped in .whatsnext assumed an existing crypto helper or simple-to-add approach. Reality: `grep -rn passwordEnc|secretKeyEnc|webhookTokenEnc` returned ZERO matches across all source code — the *Enc columns are schema placeholders that have never been wired up. No crypto module exists. Only crypto usage anywhere is `timingSafeEqual` (webhook) and `randomUUID` (storage paths). bcryptjs is in deps but that's one-way hashing, wrong tool. AskUserQuestion presented 4 options (split, trim, pivot-C, pivot-D); user chose split. Direction F became 21a/b/c chain.
  - CRYPTO PRIMITIVE DECISIONS (locked in DECISIONS_LOG via this entry's lessons): AES-256-GCM via node:crypto built-in; 32-byte base64 key in APP_ENCRYPTION_KEY env var; 12-byte random IV per encryption (NIST SP 800-38D); 16-byte auth tag mandatory; format `{iv}.{tag}.{ct}` dot-separated base64url; no key versioning prefix in v1 (YAGNI — add `v2.` when rotation forced).
  - LAZY ENV READ PATTERN: getKey() reads process.env.APP_ENCRYPTION_KEY at CALL time, not module-import time. Lets tests manipulate env var per-test via beforeEach/afterEach without vi.mock complexity. Matches @/lib/xendit pattern (getXenditClient reads process.env at call time).
  - FILE-LEVEL RED CONFIRMATION: vitest reported file-level fail "Cannot find module '@/lib/crypto'" — entire test file failed at import, tests reported as "no tests" (file couldn't load). This IS a valid RED — clear signal that the module doesn't exist. After implementation, file loads and per-test resolves normally to 9 GREEN.
  - PRE-FLIGHT SCOPE-SHRINK: .env.example does NOT exist in this project (Bootstrap template mentions it but this project has only .env.dev / .env.staging / .env.prod + CREDENTIALS.md). Reduced batch from 5 files to 4 files. Documented as a 🟢 lesson for future batches that try to update .env.example.
  - 🔴 CRITICAL FOR NEXT BATCH: Before 21b or 21c ships to staging/prod, human MUST generate APP_ENCRYPTION_KEY for each environment (different keys per env — never reuse) and add to .env.staging + .env.prod. Without this, env.ts will fail at boot. Flagged in CHANGELOG_AI lessons.

PRIOR BATCH 20 baseline 697/697 GREEN → Batch 21a 706/706 GREEN. Test files: 23 (was 22, +1 crypto.test.ts). Cumulative test delta from Batch 14 baseline (648) to now (706): +58 tests across 8 batches (3 cancel-release + 5 fulfillment + 5 createXenditInvoice + 8 webhook + 16 cart-reducer + 7 placeOrderAsCustomer + 5 Xendit-for-guests + 9 crypto).

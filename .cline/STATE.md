# Project State — Orqafy
# Auto-generated by Claude Code. Never edit manually.
# Updated: 2026-07-12 by CLAUDE_CODE (RBAC §4 — owner-transfer UI, two-way succession §2, Full Auto)

PHASE:        RBAC §4 ROLLOUT — DONE. Un-gated [HOW] queue GENUINELY EXHAUSTED for this goal.
              This Full-Auto session built the last remaining un-gated piece: the owner-transfer
              UI (two-way succession §2 — the transferOwnership procedure + tests already existed
              from M2; only the in-app UI was missing). role.delete (0270086) + this succession UI
              (f89e689) together complete the §4 user-facing surface. All LOCAL on
              feat/tenant-rbac-3tier, HARD HOLD (unpushed). HEAD f89e689 (+ a docs commit to
              follow). Remaining work is owner-gated [WHAT] only (see NEXT).
LAST_DONE:    1 code commit this Full-Auto session (after d06f733):
              • f89e689 — RBAC §4 owner-transfer UI. Added the in-app path for a tenant owner to
                hand over ownership (§2 two-way succession). user router now exposes isTenantOwner
                from user.list + user.byId. settings/users gets an owner-only "Ownership" panel
                with a Transfer-ownership dialog (shadcn Select of eligible members = active,
                non-owner, non-self; two-step confirm; toast + refresh) + a data-driven "Owner"
                badge on the owner's row. New client component transfer-ownership.tsx mirrors
                deactivate-button.tsx. Platform break-glass reassignTenantOwner UI deliberately
                NOT built (no platform console in this app; break-glass stays API/ops-only).
                No schema/migration, no reseed.
              ── prior history (unchanged) ── 0270086 (role.delete — custom-role CRUD complete) ·
              cb0c783 (USERS+PAYROLL hardening) · git log 75bc162..d7e1f5a for M5–M9 + RBAC §4
              Tracks A/B/C (rollout 23/35 COMPLETE).
GATE (PM ground-truth, this session): web typecheck 0 · web vitest 1258/1258 · web eslint 0 ·
              lint-design.sh --report-only PASS. Live Rule-16 Visual QA (dev :42951, demo tenant):
              owner (webmaster, is_tenant_owner) sees the "Ownership" panel + "Owner" badge; the
              Transfer dialog lists exactly the 2 eligible members (admin, user) with confirm gated
              on selection (canceled without executing to preserve the fixture); non-owner
              (admin@mail.com, Tenant Super Admin) sees NO panel but still sees the Owner badge
              (data-driven). 0 console errors. QA login used a reversible dev-only password-hash
              swap for webmaster (restored after; never read CREDENTIALS.md).
NEXT:         Owner-facing. This RBAC §4 goal is DONE and the un-gated [HOW] queue is exhausted.
              Remaining is owner-gated [WHAT] only:
              (1) Owner-gated (docs/PENDING_DECISIONS.md): reseed live/staging/prod, apply
                  migrations 20260710160000 + 20260711010000 to staging/prod, framework-sync
                  push, git-tag push (next = MINOR 0.11.0), staging/prod/demo deploy, D-PRIV-1
                  (RA 10173), D-NUM-1.
              (2) OPTIONAL, does NOT hold the loop open: a PLATFORM break-glass console for
                  platform.reassignTenantOwner (cross-tenant tenant_manager reassign). NOT built —
                  no platform UI area exists in this app and break-glass legitimately stays
                  API/ops-only; building a cross-tenant console would be speculative scope.
                  role.delete DONE (0270086). Owner-transfer UI DONE (f89e689). Both §4 custom-role
                  CRUD and §2 two-way succession are now fully usable in-app.
              ANTI-REGRESSION RULE (standing): the seed packages/db/src/seed/role-permissions.ts is
              the AUTHORITATIVE endpoint→action map. Matrix tests run under Platform-Owner BYPASS, so
              they do NOT catch mis-gating — PM must diff each endpoint→action against the seed + add
              a non-bypass allow/deny test. Do NOT re-audit IDOR (33/33, M8) or re-run P1/P2 (M5/6/7).
              Login (dev): admin@mail.com/admin (=Tenant Super Admin, demo) · user@mail.com/user
              (=Staff) · webmaster@orqafy.local (=Tenant Super Admin, pw in CREDENTIALS.md).
              App http://localhost:42951, tenant URL /demo/... Screenshots → screenshots/ (gitignored).
BLOCKERS:     none for [HOW] (queue GENUINELY EXHAUSTED for this RBAC goal). Owner-gated HARD HOLD.
GIT_BRANCH:   feat/tenant-rbac-3tier (unpushed — HARD HOLD). HEAD f89e689 (owner-transfer UI) + a
              docs commit on top; prior 0270086/d06f733 (role.delete), cb0c783, 496ec0d; after
              v0.10.0 tag @ d2d6196.
DEV STACK:    UP (db 42941 / redis 42943 / app 42951 hot-reload). Host-side DB: build DATABASE_URL
              from DB_USER/DB_PASSWORD/localhost:42941/DB_NAME (.env.dev uses ${..} Prisma won't
              resolve). psql: docker exec orqafy_dev_postgres.
OWNER-GATED (docs/PENDING_DECISIONS.md): D-MIG-APPLY · D-RBAC-C1..C4 (Wave C RBAC + reseed,
              ANSWERED) · D-PRIV-1 (RA 10173) · framework-sync push · git-tag push ·
              staging/prod/demo deploy · D-1/3/4 product scope · D-NUM-1.
              (D-RBAC-USERS-UNGATED + D-RBAC-PAYROLL-UNGATED RESOLVED 2026-07-12, commit cb0c783.)
MODELS:
  planning:   claude-opus (PM/architect)
  execution:  spec-executor (Sonnet) workers
  governance: claude-code

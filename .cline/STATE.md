# Project State — Orqafy
# Auto-generated by Claude Code. Never edit manually.
# Updated: 2026-07-11 by CLAUDE_CODE (RBAC §4 — Tracks A+B+C all substantively COMPLETE this session)

PHASE:        RBAC §4 ROLLOUT — the owner selected all three tracks (A nav-filter · B role-builder ·
              C resolve-deferred-routers) and they are ALL DONE + PM-ground-truth verified + live
              Visual-QA'd this session. All LOCAL on feat/tenant-rbac-3tier, HARD HOLD (unpushed).
              HEAD d7e1f5a. NOT the whole-project end — remaining items are owner-gated [WHAT]s
              (reseed/deploy) + 2 pre-existing security gaps flagged for owner (see NEXT).
LAST_DONE:    3 reviewed commits this session (after 1bad6d1):
              • e3d8f1f — Track C: migrated accounting/purchasing/storefront routers to the
                permission matrix (rollout 23/35). Owner [WHAT] rulings applied (all "recommended"):
                  – accounting: writes → matrix create/update = Accountant + bypass (tightens the
                    ungated broad writes; fixes the dead-name accountantWriteProcedure).
                  – purchasing: reactivate + approve → matrix `delete` (Admin + Purchasing Staff +
                    bypass); FIXES po.approve (was a dead-name gate 403ing everyone incl. bypass).
                  – storefront: admin actions (listAllOrders/updateFulfillment/updateOrderStatus/
                    createXenditInvoice) → matrix `update` = bypass-only (Tenant Super Admin +
                    Platform Owner); fixes dead "Administrator" gate + widens to TSA per ruling.
                  – dsr: RESOLVED as NO code change — its admin queue keeps its real-name requireRole
                    gate (self-service already open). Routing to matrix would over-widen the RA-10173
                    queue (view broad) or lock out Admin (update false) without an owner-gated reseed.
                ZERO seed change / ZERO reseed — existing role_permissions rows already encode these
                grants; migration only moves enforcement to read the matrix. Each endpoint→action
                diffed against the authoritative seed. 3 new *-matrix.test.ts (NON-BYPASS allow/deny)
                + 9 existing business-logic/tenant-parity tests updated to the matrix model
                (real hasPermission + role.findFirst bypass), old role-gate intent preserved via
                matrix deny-cases.
              • f5092a6 — Track A: sidebar-nav filters NAV_ITEMS by matrix `view` via the existing
                role.myPermissions query (bypass→all; deny-by-default Skeleton while pending; FeatureKey
                import type-only). ALSO fixed a latent M9 build regression: packages/shared/src/rbac/
                index.ts re-exported "./features.js"/"./actions.js" (the ONLY shared index using .js;
                siblings are extensionless under moduleResolution:bundler). tsc+vitest resolved it but
                the Next dev bundler 500'd the trpc route on any VALUE import (role.ts) —
                "Can't resolve ./features.js". Changed to extensionless. Caught by live Visual QA
                (role.myPermissions is the first client→role-router call). Global lesson logged.
              • d7e1f5a — Track B: TSA-only role-builder UI at /settings/roles (backend already
                existed). page.tsx guardPage("settings") + TSA/PO redirect gate; roles-client.tsx =
                FEATURE_KEYS×{view,create,update,delete} accessible checkbox matrix prefilled from
                role.list; users/billing rows DISABLED "Reserved for owner"; save→role.create/update
                with guardrail errors surfaced as toasts; shadcn Skeleton loading. settings hub gains
                a "Roles & Permissions" card shown ONLY to TSA/PO.
              MATRIX FEATURE-ROUTER ROLLOUT IS NOW COMPLETE: all 23 feature routers enforce via the
              matrix; dsr resolved. The un-migrated routers (auth, user, tenant, platform, plan,
              registration, notification, storage, demo, role) are intentionally NOT matrix-gated
              (public / global / self-scoped / platform / TSA-gated).
              ── prior history (unchanged) ── see git log 75bc162..1bad6d1 for M5–M9 + Track C 20/35.
GATE (PM ground-truth, this session): web typecheck 0 · web vitest 1242/1242 · web lint 0 warnings ·
              lint-design PASS · @orqafy/db 61 · @orqafy/shared typecheck+4 tests. Live Visual QA on
              dev (app 42951): Admin(=TSA) sees full filtered nav + role-builder renders + matrix
              prefills per seed + users/billing locked + 0 console errors; Staff redirected off
              /settings/roles + Roles card hidden. Build error fixed (was 500, now clean).
NEXT:         Owner-facing. This RBAC §4 goal is substantively DONE. Remaining is owner-gated:
              (1) ⚠ TWO pre-existing security gaps flagged in the seed (owner [WHAT] — NOT auto-fixed):
                  • D-RBAC-USERS-UNGATED — user.ts list/byId/deactivate are plain protected/write
                    procedures: ANY authenticated tenant member can list/view/deactivate any other
                    user. Fleet standard reserves Users for TSA/PO. Fix = gate user.ts to the matrix
                    `users` feature / TSA. Owner call: harden now?
                  • D-RBAC-PAYROLL-UNGATED — payroll.ts is fully ungated (create/update/delete open to
                    all staff) though legacy intent was HR-Manager-only. Tighten to a payroll seed
                    grant? Owner call.
              (2) Owner-gated (docs/PENDING_DECISIONS.md): reseed dev/staging/prod, apply migrations
                  20260710160000 + 20260711010000 to staging/prod, framework-sync push, git-tag push
                  (next = MINOR 0.11.0), staging/prod/demo deploy, D-PRIV-1 (RA 10173), D-NUM-1.
              (3) Optional [HOW] follow-ups if owner wants: role.delete procedure (role-builder can
                  create/edit but not delete a custom role — currently no delete endpoint); a
                  succession/assign UI. Not required by the standard.
              ANTI-REGRESSION RULE (standing): the seed packages/db/src/seed/role-permissions.ts is
              the AUTHORITATIVE endpoint→action map. Matrix tests run under Platform-Owner BYPASS, so
              they do NOT catch mis-gating — PM must diff each endpoint→action against the seed + add
              a non-bypass allow/deny test. Do NOT re-audit IDOR (33/33, M8) or re-run P1/P2 (M5/6/7).
              Login (dev): admin@mail.com/admin (=Tenant Super Admin, demo) · user@mail.com/user
              (=Staff) · webmaster@orqafy.local (=Tenant Super Admin, pw in CREDENTIALS.md).
              App http://localhost:42951, tenant URL /demo/... Screenshots → screenshots/ (gitignored).
BLOCKERS:     none for [HOW] (queue exhausted for this RBAC goal). Owner-gated remain HARD HOLD.
GIT_BRANCH:   feat/tenant-rbac-3tier (unpushed — HARD HOLD). HEAD d7e1f5a (3 commits this session:
              e3d8f1f, f5092a6, d7e1f5a; rollout 23/35 + Tracks A & B; after v0.10.0 tag @ d2d6196).
DEV STACK:    UP (db 42941 / redis 42943 / app 42951 hot-reload). Host-side DB: build DATABASE_URL
              from DB_USER/DB_PASSWORD/localhost:42941/DB_NAME (.env.dev uses ${..} Prisma won't
              resolve). psql: docker exec orqafy_dev_postgres.
OWNER-GATED (docs/PENDING_DECISIONS.md): D-MIG-APPLY · D-RBAC-C1..C4 (Wave C RBAC + reseed) ·
              D-RBAC-USERS-UNGATED · D-RBAC-PAYROLL-UNGATED · D-PRIV-1 (RA 10173) · framework-sync
              push · git-tag push · staging/prod/demo deploy · D-1/3/4 product scope · D-NUM-1.
MODELS:
  planning:   claude-opus (PM/architect)
  execution:  spec-executor (Sonnet) workers
  governance: claude-code

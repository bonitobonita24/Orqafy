# Project State — Orqafy
# Auto-generated by Claude Code. Never edit manually.
# Updated: 2026-07-11 by CLAUDE_CODE (Full-Auto M3+M4 — AIEF audit + P0 fix done + committed)

PHASE:        Full-Auto run (slot-4). M1 ✅ M2 [HOW]-scope ✅ M3 ✅ M4 ✅. NEXT: M5 (headless hardening).
LAST_DONE:    M3 AIEF governance audit (3 parallel read-only agents vs V32.18 surfaces) → docs/AIEF_AUDIT_TODO.md
              (P0/P1/P2, reconciled vs existing docs). Applied fixes (commit 2a0e9ca): P0 demoRouter.reset
              cross-tenant deleteMany({}) wipe → tenant-scoped by ctx.tenantId + null-guard (+3 tests in
              demo-reset-tenant-scope.test.ts); P2 schedule-digests cron secret → timingSafeEqual; a11y
              (aria-labels on attachment download/delete + file-upload remove + sidebar nav; global
              prefers-reduced-motion in globals.css; sidebar footer contrast dropped /50). Verified:
              web 1063/1063 · typecheck · lint · lint-design all green. M4 wrap: D-PRIV-1 (RA 10173
              DPO/NPC/PIA) surfaced to PENDING_DECISIONS; CHANGELOG/plan/STATE updated. HARD HOLD — LOCAL only.
NEXT:         M5 — headless security hardening (no running stack; verify via typecheck + 1063 suite):
              S-P2a tenant-model reconciliation (remove/neutralize dormant schema-per-tenant machinery —
              zero runtime callers, root cause of the P0 — + guard SET search_path interpolation; document
              single public-schema+tenantId contract; clean test-only refs); S-P1b Zod .strict() sweep
              (~32 routers, spec-executor); D-P2 lint-design all-caps tracking. Then M6 dev-up session
              (apply migration 20260710160000 + Visual QA: Entry-1 container, mobile sidebar, rate-limiting).
BLOCKERS:     none [HOW]-wise for M5. M6 needs LOCAL dev stack up (batched — owner word / deliberate rebuild).
GIT_BRANCH:   feat/tenant-rbac-3tier (unpushed — HARD HOLD). HEAD 2a0e9ca.
OWNER-GATED (PENDING_DECISIONS.md): D-RBAC-C1..C4 (Wave C RBAC + reseed) · D-RBAC-B-APPLY migration-apply ·
              D-PRIV-1 (RA 10173 DPO/NPC/PIA) · framework-sync push · git-tag push · staging+prod deploy ·
              D-1/D-3/D-4 product scope.
MODELS:
  planning:   claude-opus (PM/architect)
  execution:  spec-executor (Sonnet) workers
  governance: claude-code

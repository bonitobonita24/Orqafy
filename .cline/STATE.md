# Project State — Orqafy
# Auto-generated by Claude Code. Never edit manually.
# Updated: 2026-07-12 by CLAUDE_CODE (RBAC §4 — user-management + payroll hardening, owner-approved)

PHASE:        RBAC §4 ROLLOUT — substantively DONE. The two pre-existing security gaps flagged
              for the owner (D-RBAC-USERS-UNGATED, D-RBAC-PAYROLL-UNGATED) were approved and
              CLOSED this session (commit cb0c783). All LOCAL on feat/tenant-rbac-3tier, HARD
              HOLD (unpushed). HEAD cb0c783. Remaining is owner-directed / owner-gated only
              (see NEXT) — no un-gated [HOW] work queued for this goal.
LAST_DONE:    1 commit this session (after d7e1f5a):
              • cb0c783 — RBAC §4 hardening, owner-approved 2026-07-12: (1) D-RBAC-USERS-UNGATED
                RESOLVED — user.ts (list/byId/deactivate) gated to a FIXED Tenant Super
                Admin/Platform Owner check (NOT the matrix — Users is guardrail-forbidden/
                reserved per fleet standard §4); fixed the underlying superAdminProcedure gap;
                added /settings/users TSA-PO redirect gate + hid the Users card from non-TSA/PO.
                Immediate effect, no reseed. (2) D-RBAC-PAYROLL-UNGATED RESOLVED — payroll.ts
                router was already matrix-migrated; tightened the SEED grant
                (role-permissions.ts) so payroll create/update/delete = HR Manager + bypass
                only (was all internal staff), mirroring dtr/employees. Dev reseeded
                (idempotent `pnpm db:seed`) — verified at the data layer. succession.test.ts
                denies Staff and non-owner Admin on all three user.ts endpoints.
              ── prior history (unchanged) ── see git log 75bc162..d7e1f5a for M5–M9 + RBAC §4
              Tracks A/B/C (rollout 23/35 COMPLETE — all 23 feature routers enforce via the
              matrix; Track A nav-filter + Track B role-builder UI shipped).
GATE (PM ground-truth, this session): web typecheck 0 · web vitest 1253/1253 · web eslint 0
              warnings · lint-design.sh --report-only PASS · @orqafy/db 61/61 + typecheck 0.
              Live QA: Staff redirected off /settings/users, Users card hidden from non-TSA/PO.
              succession.test.ts denies Staff and non-owner Admin on all three user.ts endpoints.
NEXT:         Owner-facing. This RBAC §4 goal is substantively DONE — owner chose to hold here.
              Both previously-open security gaps (D-RBAC-USERS-UNGATED, D-RBAC-PAYROLL-UNGATED)
              are CLOSED. Remaining is owner-gated only:
              (1) Owner-gated (docs/PENDING_DECISIONS.md): reseed live/staging/prod, apply
                  migrations 20260710160000 + 20260711010000 to staging/prod, framework-sync
                  push, git-tag push (next = MINOR 0.11.0), staging/prod/demo deploy, D-PRIV-1
                  (RA 10173), D-NUM-1.
              (2) Optional [HOW] follow-ups if owner wants: role.delete procedure (role-builder
                  can create/edit but not delete a custom role — currently no delete endpoint);
                  a succession/assign UI. Not required by the standard.
              ANTI-REGRESSION RULE (standing): the seed packages/db/src/seed/role-permissions.ts is
              the AUTHORITATIVE endpoint→action map. Matrix tests run under Platform-Owner BYPASS, so
              they do NOT catch mis-gating — PM must diff each endpoint→action against the seed + add
              a non-bypass allow/deny test. Do NOT re-audit IDOR (33/33, M8) or re-run P1/P2 (M5/6/7).
              Login (dev): admin@mail.com/admin (=Tenant Super Admin, demo) · user@mail.com/user
              (=Staff) · webmaster@orqafy.local (=Tenant Super Admin, pw in CREDENTIALS.md).
              App http://localhost:42951, tenant URL /demo/... Screenshots → screenshots/ (gitignored).
BLOCKERS:     none for [HOW] (queue exhausted for this RBAC goal). Owner-gated remain HARD HOLD.
GIT_BRANCH:   feat/tenant-rbac-3tier (unpushed — HARD HOLD). HEAD cb0c783 (1 commit this
              session on top of d7e1f5a; after v0.10.0 tag @ d2d6196).
DEV STACK:    UP (db 42941 / redis 42943 / app 42951 hot-reload). Host-side DB: build DATABASE_URL
              from DB_USER/DB_PASSWORD/localhost:42941/DB_NAME (.env.dev uses ${..} Prisma won't
              resolve). psql: docker exec orqafy_dev_postgres.
OWNER-GATED (docs/PENDING_DECISIONS.md): D-MIG-APPLY · D-RBAC-C1..C4 (Wave C RBAC + reseed,
              ANSWERED) · D-PRIV-1 (RA 10173) · framework-sync push · git-tag push ·
              staging/prod/demo deploy · D-1/3/4 product scope · D-NUM-1.
              (D-RBAC-USERS-UNGATED + D-RBAC-PAYROLL-UNGATED RESOLVED 2026-07-12, commit cb0c783.)
MODELS:
  planning:   claude-opus (PM/architect)
  execution:  spec-executor (Sonnet) workers
  governance: claude-code

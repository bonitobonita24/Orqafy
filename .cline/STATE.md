# Project State — Orqafy
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-05-29 GMT+8 by CLAUDE_CODE Opus 4.7 (Batch 27 CLOSE — Direction I-2 COMPLETE: PurchaseOrderItem.tenantId parity — schema + JOIN-backfill migration + router create-site injection + 1 RED→GREEN test)

PHASE:        ✅ Phase 8 Batch 27 COMPLETE — Direction I-2 SHIPPED end-to-end. Four Sonnet 4.6 dispatches under V32 Zero Opus Execution: I-2-1 RED test (1 assertion on tx.purchaseOrderItem.create data.tenantId — commit 140f0ef; verified RED via vitest -t "Direction I-2 PurchaseOrderItem" before commit). I-2-2 schema + migration (PurchaseOrderItem.tenantId field + tenant relation + @@index([tenantId]); Tenant.purchaseOrderItems back-pointer; migration 20260529110000_add_tenant_id_to_purchase_order_items using 5-step JOIN-backfill from purchase_orders ON purchase_order_id — Batch 24 EcommerceOrderItem pattern reapplied since PurchaseOrder.tenantId is NOT NULL from Batch 26; commit b83e76e; prisma client regenerated clean 0 errors). I-2-3 router GREEN (1 surgical edit at purchasing.ts ~L350: `tenantId: ctx.tenantId,` injected into tx.purchaseOrderItem.create data — minimal-diff principle; commit c042f0f; 748/748 GREEN, typecheck 0 errors, lint 0 errors). I-2-4a governance close (CHANGELOG_AI.md Batch 27 entry appended after Batch 26 newest-at-bottom; IMPLEMENTATION_MAP.md Next Action header rewritten to 748 GREEN + Batch 27 promoted to item 1 with Batch 26 demoted to item 2; .whatsnext header updated, deploy gates expanded to 6 with new gate for migration 27, I-2 marked COMPLETE, I-3 PurchaseOrderItemAllocation promoted to ★ recommended next with refined rationale citing item_id JOIN now that poi.tenantId is NOT NULL; lessons.md 2 typed entries — 🟢 JOIN-backfill canonical pattern for child-with-parented-tenant + 🟤 grandchild deferral discipline preserves batch tier-1 scope; commit 750b45b). Squash-merge to main pending Opus.

HANDOFF:      none (clean close — Direction I-2 COMPLETE). PRE-FLIGHT FINDING confirmed: PurchaseOrder.tenantId from Batch 26 was a clean parent backfill source — JOIN UPDATE produced 0 NULL rows (all child items have parent), no orphan handling needed in migration. 🟢 PATTERN BANKED: JOIN-backfill from already-tenant-scoped parent is canonical for any child-of-parented-model parity (Batch 24 EcommerceOrderItem → Batch 27 PurchaseOrderItem confirms 2 independent applications; Direction I-3 PurchaseOrderItemAllocation + Direction I-5 sibling wave all reuse). DELIBERATELY DEFERRED in this batch (logged 🟤 in lessons): (a) tx.purchaseOrderItemAllocation.create at L362 — grandchild, Direction I-3 next batch; (b) tx.purchaseOrderItem.update at L669 + findMany at L676 — inside po-scoped txns where parent already tenant-guarded by Batch 26, defense-in-depth tenant injection deferred to L6 Prisma guardrails workstream; (c) PurchaseOrder status-transition sibling guards (update/submit/approve/markOrdered/cancel) — Direction I-4 needs RED tests first per Rule 25 TDD discipline; loadPoForTenant helper banked from Batch 26 security review. SIX DEPLOY GATES on staging/prod: (1) APP_ENCRYPTION_KEY in .env.staging + .env.prod + Komodo restart, (2) prisma migrate deploy of 21c (ecommerce_orders.tenantId), (3) prisma migrate deploy of 22 (webhookProcessedAt), (4) prisma migrate deploy of 24 (ecommerce_order_items.tenantId), (5) prisma migrate deploy of 26 (purchase_orders.tenantId), (6) NEW prisma migrate deploy of 27 (purchase_order_items.tenantId). See docs/deployment-direction-f.md (gate 6 to be added in follow-on doc-only batch if needed before next deploy).

TOKEN_ESTIMATE: ~35K Opus 4.7 architect-only session — pre-flight ctx_batch_execute (10 cmds scout) + BUILD BATCH PROPOSAL with verified Tier 1 classification + 4 Sonnet 4.6 dispatches (I-2-1, I-2-2, I-2-3, I-2-4a) + 1 second-tier scout for governance anchors + STATE.md self-write (Opus exception V32 R1) + bash squash-merge. No thrash. No /clear. No re-decomposition. No path-fix retry (Batch 26 mount-key drift lesson applied — Sonnet prompts pinned `purchasing.po.*` path from the start). Sonnet sub-task token totals: I-2-1 ~167K + I-2-2 ~171K + I-2-3 ~167K + I-2-4a ~178K = ~683K subagent tokens total across executor dispatches.

CHECKPOINT_TYPE: full
TIER_CLASSIFICATION: T1 Lightweight per memory-governance §1 — 4 files / 1 module (purchasing) / ~75 lines net build edits + ~95 lines governance writes. Matches BUILD BATCH PROPOSAL Option A estimate (~7K direct + 1 governance file pass). V32 R3 line ranges used throughout: schema.prisma (2138L → 2 targeted blocks: Tenant @ L50, PurchaseOrderItem @ L776-798), purchasing.ts (708L → 1 surgical edit at L350), purchasing.test.ts (776L→809L append-only). V32 R5 Sonnet Scout applied via 2 ctx_batch_execute scout batches before any dispatch (initial scope + governance anchors).

FILES_TOUCHED (Batch 27, 4 commits on branch + squash-merge pending):
  Build commits (3):
    140f0ef (I-2-1 RED test):
      - apps/web/src/__tests__/purchasing.test.ts (+~30 lines: 1 RED test in new describe block `purchasing.po — Direction I-2 PurchaseOrderItem tenantId scoping (RED)`; assertion on tx.purchaseOrderItem.create receiving data.tenantId === ctx.tenantId; file 776 → 809 lines)
    b83e76e (I-2-2 schema + migration):
      - packages/db/prisma/schema.prisma (+3 lines: PurchaseOrderItem.tenantId + tenant relation + @@index([tenantId]); +1 line: Tenant.purchaseOrderItems back-pointer)
      - packages/db/prisma/migrations/20260529110000_add_tenant_id_to_purchase_order_items/migration.sql (NEW, ~30 lines — 5-step ADD-nullable → UPDATE FROM purchase_orders JOIN on purchase_order_id → SET NOT NULL → FK ON DELETE RESTRICT ON UPDATE CASCADE → INDEX; Batch 24 pattern, NOT Batch 26 ORDER BY LIMIT-1)
    c042f0f (I-2-3 router scoping — GREEN):
      - apps/web/src/server/trpc/routers/purchasing.ts (1 surgical edit at ~L350: `tenantId: ctx.tenantId,` inserted into tx.purchaseOrderItem.create data object)
  Governance commit (1):
    750b45b:
      - docs/CHANGELOG_AI.md (Batch 27 entry appended; newest-at-bottom convention preserved)
      - docs/IMPLEMENTATION_MAP.md (Next Action header rewritten to 748 GREEN + Batch 27 promoted item 1; Batch 26 demoted item 2)
      - .whatsnext (3 surgical replaces: title header → Batch 27 COMPLETE; deploy gates 5→6 with new migration 27 row; I-2 entry marked COMPLETE + I-3 ★ recommended next with refined item_id JOIN rationale)
      - .cline/memory/lessons.md (+2 typed entries: 🟢 JOIN-backfill canonical pattern for child-with-parented-tenant + 🟤 grandchild deferral discipline preserves batch tier-1 scope)

DIRECTION_I_PROGRESS:
  ✅ I-1     PurchaseOrder RED tests + schema + router scoping — Batch 26 (28f5d9f)
  ✅ I-2     PurchaseOrderItem.tenantId parity — Batch 27 (this batch — squash-merge pending)
  ⬜ I-3     PurchaseOrderItemAllocation.tenantId — ★ recommended next (T1 ~6K; grandchild JOIN-backfill from PurchaseOrderItem.tenantId via item_id)
  ⬜ I-4     PurchaseOrder status-transition guards on update/submit/approve/markOrdered/cancel — Rule 25 RED tests first; loadPoForTenant helper banked (T1 ~6K)
  ⬜ I-5a    Sibling wave: ShippingCost + ShippingCostDistribution + GoodsReceipt (T2 ~12K)
  ⬜ I-5b    Sibling wave: GoodsReceiptItem + PurchaseInvoice (T2 ~10K)
  ⬜ Vendor  Q2 catalogue model; low blast radius; revisit after items/allocations/siblings

PURCHASING_SURFACE_PROGRESS (post-Batch-27):
  ✅ PurchaseOrder                  — Batch 26 (tenantId column + scoped list/byId/create)
  ✅ PurchaseOrderItem              — Batch 27 (tenantId column + create-site injection; JOIN-backfill from PO)
  ⬜ PurchaseOrderItemAllocation    — queued as Direction I-3 (grandchild JOIN-backfill from POI) ★ recommended next
  ⬜ PurchaseOrder status guards    — queued as Direction I-4 (RED tests first per Rule 25, T1 ~6K)
  ⬜ ShippingCost                   — queued as Direction I-5a
  ⬜ ShippingCostDistribution       — queued as Direction I-5a
  ⬜ GoodsReceipt                   — queued as Direction I-5a
  ⬜ GoodsReceiptItem               — queued as Direction I-5b
  ⬜ PurchaseInvoice                — queued as Direction I-5b
  ⬜ Vendor                         — Q2 catalogue, lowest priority

FULL_ECOMMERCE_ARC_PROGRESS (carried — unchanged this batch):
  ✅ 21a Encryption infrastructure (976df52)
  ✅ 21b TenantXenditConfig CRUD + admin UI (ff61bea)
  ✅ 21c Per-tenant Xendit consumer refactor + EcommerceOrder.tenantId (abe57ce)
  ✅ Batch 22 webhookProcessedAt
  ✅ Batch 23 Direction D — guest tracking + audit log + admin filters
  ✅ Batch 24 Direction G — EcommerceOrderItem.tenantId parity
  ✅ Batch 25 Direction H — pino logger + APP_ENCRYPTION_KEY startup health check + payment/webhook/admin log lines
  ✅ Batch 26 Direction I-narrow — PurchaseOrder.tenantId parity
  ✅ Batch 27 Direction I-2 — PurchaseOrderItem.tenantId parity (this batch)

NEXT_RECOMMENDED: Direction I-3 (PurchaseOrderItemAllocation.tenantId via JOIN-backfill from PurchaseOrderItem.tenantId on item_id — same shape as Batch 27, T1 ~6K, single-Sonnet-dispatch per sub-task expected). Alternative: Direction I-4 (PO status-transition guards — needs RED tests first, loadPoForTenant helper banked). Defer Direction I-5 (sibling wave) until I-3 + I-4 ship.

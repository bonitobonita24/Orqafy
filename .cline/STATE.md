# Project State — Orqafy
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-05-31 21:18 GMT+8 by CLAUDE_CODE Opus 4.7 (P1 — 4 of 6 stubs shipped: + invoices + expenses)

PHASE:        ✅ P1 STUBS 4/6 SHIPPED — invoices + expenses added this turn on top of clients-redirect + settings landing from earlier session. Per dev-first feedback ([[feedback_dev_before_staging]]) + prior STATE.md NEXT block. Three Sonnet dispatches all clean (V32 R1 + R2 + V32.1 op note, no thrash): (a) docker-compose.app.hot.yml + COMMANDS.md Hot-Reload Dev Mode subsection, (b) clients/page.tsx → redirect + sidebar Clients drop + Users icon prune, (c) settings/page.tsx 14L stub → 155L real landing per PRODUCT.md lines 1623-1627 + 1764-1765. Browser-verified via Playwright: sidebar 19 items (no Clients), settings landing renders with workspace card (Orqafy Demo / slug=demo / status=demo) + 6 config-area cards (Xendit = Live link, others = inert Coming-soon divs).

PRIOR_PHASE:  Dev audit + P0 sidebar nav fix (commit ef621e3, pushed in this session as part of 1557b2f bundle) — earlier this same calendar day 2026-05-31 morning + late afternoon. P0 surfaced 9 orphan modules + flagged 6 STUB pages as P1 punch list.

HANDOFF:      none (clean stop at user-chosen "pause + checkpoint" gate). Hot-reload variant deliberately LEFT RUNNING for next session — see PORTS block.

P1_SHIPPED_THIS_SESSION (3 line items):

  1. Hot-reload dev compose variant (P3 prereq from prior STATE.md)
     File: deploy/compose/dev/docker-compose.app.hot.yml (45 lines)
     Image: node:22-alpine (no Dockerfile build — direct alpine + pnpm install at runtime)
     Volumes: bind ../../../ → /workspace + 3 named volumes (app_hot_node_modules / app_hot_next / app_hot_turbo) to cache deps + .next + .turbo across container restarts
     Command: npm install -g pnpm@10 && pnpm install --frozen-lockfile && pnpm --filter @orqafy/db db:generate && pnpm --filter @orqafy/web dev
     Container name: ${COMPOSE_PROJECT_NAME}_app_hot (coexists with regular app config but cannot bind ${APP_PORT}=42951 simultaneously — stop one before the other)
     NO depends_on: postgres/valkey live in separate compose files; Docker rejects cross-file refs. Comment in file + COMMANDS.md ordering instructions cover the gap.
     Boot-tested: docker compose config -q clean; first boot ~60s (pnpm install of 1680 packages inside container) → /api/health HTTP 200 in 44ms warm; next dev "Ready in 1944ms"; settings route compiled middleware in 1520ms (288 modules) on first hit.
     COMMANDS.md: new "### Hot-Reload Dev Mode (NEW)" subsection inserted in "Docker — Start / Stop / Rebuild" section with start/stop/logs/down table + 3 caveats.

  2. Clients → /crm/customers redirect (P1 stub 1 of 6)
     PRODUCT.md verification: 2160 lines scanned; Customer is the ONLY entity (CRM/Sales section lines 204-260); "client" appears only as colloquial Project-payments shorthand (line 641: "Client Information: linked to Customer entity") and "client-side" browser code. NO Clients module/entity/page declared anywhere. Sidebar Clients was Phase 4 framework scaffold, not deliberate product opinion.
     apps/web/src/app/(tenant)/[slug]/(app)/clients/page.tsx — 11L stub → 11L async server redirect to /{slug}/crm/customers via redirect() from next/navigation, awaits params: Promise<{slug:string}> per Next 15 pattern.
     DELETED: clients/loading.tsx (9L) + clients/error.tsx (22L) — no async work to load, no error boundary needed beyond parent layout.
     apps/web/src/components/layout/app-sidebar.tsx — removed { label: "Clients", href: "clients", icon: Users } from NAV_ITEMS + dropped Users from lucide-react import list (sole usage in file). Sidebar 20 → 19 items.
     Browser-verified: snapshot of navigation list at /demo/dashboard shows exactly 19 items in this order: Dashboard, CRM (/demo/crm/customers), Projects, Job Orders, Tasks, Support, Invoices, Expenses, Accounting, Banking, Employees, DTR, Payroll, Inventory, Purchasing, POS, Ecommerce, Reports, Settings. No Clients.

  3. Settings landing (P1 stub 2 of 6)
     apps/web/src/app/(tenant)/[slug]/(app)/settings/page.tsx — 14L stub → 155L real landing.
     Workspace context card: prisma.tenant.findUnique({where:{slug}, select: id+name+slug+status}) with ?? "—" null-safe fallbacks. Status badge uses brand-green border/bg/text (#00d992) if status === "active" else neutral muted badge with raw value (demo tenant shows "demo" badge — correct per PRODUCT.md "status = demo (new Tenant status value)").
     6 configuration-area cards in CARDS const (1 / sm:grid-cols-2 / lg:grid-cols-3 responsive grid) per PRODUCT.md lines 1623-1627 + 1764-1765:
       1. Account & Tenant (Building2 icon) — Coming soon, inert div
       2. Users (Users icon) — Coming soon, inert div [href settings/users banked for when built]
       3. Departments (FolderTree icon) — Coming soon, inert div [settings/departments]
       4. Expense Categories (Tag icon) — Coming soon, inert div [settings/expense-categories]
       5. SMTP (Mail icon) — Coming soon, inert div [settings/smtp]
       6. Xendit Payments (CreditCard icon) — LIVE, wrapped in <Link href="settings/xendit"> (real route exists from Batch 21b)
     Inert cards intentionally are <div> not disabled <Link> — clicking does nothing rather than 404. When a sub-route is built later: flip live: false → true + add href and the card auto-becomes clickable.
     Server component, dynamic = force-dynamic. No client component, no useState, no forms. Pattern mirrors accounting/employees landing pages (plain Tailwind divs, NOT shadcn Card primitive).
     Browser-verified: /demo/settings renders with workspace=Orqafy Demo + slug=demo + status="demo" badge + 6 cards in correct order; only Xendit shown as <link> in accessibility tree, others as <generic>.

P1_SHIPPED_THIS_TURN (2 line items — Opus direct execution, not Sonnet dispatch, both <200L):

  4. Invoices list (P1 stub 3 of 6)  — commit 745413e
     apps/web/src/app/(tenant)/[slug]/(app)/invoices/page.tsx — 12L stub → 150L SSR list.
     Direct prisma.invoice.findMany (no router call — mirrors customers pattern).
     Columns: invoice #, customer (companyName ?? firstName+lastName), status pill, total (Intl.NumberFormat PHP), balance, due date.
     7 status colors: draft/sent/partially_paid/paid/overdue/void/cancelled. Header summary: total · paid count · outstanding sum (sent + partially_paid + overdue).
     Decimal field unwrap via duck-typed toNumber check (avoids importing Prisma Decimal type).
     Browser-verified: empty state ("No invoices yet.") + 3 seeded rows (INV-DEMO-001/002/003 across sent/paid/draft) all render correctly with proper currency + dates + pills. Zero console errors.
     Seed: 1 Customer (Acme Corp / Jane Reyes / Makati / VIP) + 3 Invoices created directly in container via @orqafy/db dist (no tenantId — schema doesn't have it on either model yet, see Direction K).

  5. Expenses list (P1 stub 4 of 6)  — commit 4c36a7d
     apps/web/src/app/(tenant)/[slug]/(app)/expenses/page.tsx — 12L stub → 138L SSR list.
     Direct prisma.expense.findMany. Columns: expense #, description, category (expenseCategory.name), submitted-by (createdBy.firstName+lastName), status pill, amount, date.
     4 status colors: pending (yellow) / approved (blue) / rejected (red) / reimbursed (#00d992).
     Header summary: total · approved+reimbursed count · pending count + total amount.
     DELETED: expenses/loading.tsx (7L) + expenses/error.tsx (21L) — matches earlier clients-route cleanup pattern (8ac919b).
     Browser-verified: 4 seeded rows (one per status) all render with full palette, correct submitter, currency, dates.
     Seed: 2 ExpenseCategory (Office Supplies / Travel & Transport) + 4 Expense across all 4 status values.

WHAT_DID_NOT_GET_DONE (carried as next-session P1 work):
  P1 stub 5 of 6 — dashboard. Largest user-perceived impact. PRODUCT.md describes KPI cards + recent activity. ~400L single dispatch at V32 R2 cap. Should reuse the status-pill + currency-format helpers established in invoices/expenses today.
  P1 stub 6 of 6 — demo. NEEDS DESIGN PASS first per PRODUCT.md Demo System (lines 175-200): role switcher dropdown + banner + 6-hour reset — NOT a typical landing page. Defer until intent settled.

SECURITY_FINDINGS_THIS_TURN (acknowledged, not actionable until Direction K):
  Two HIGH "Multi-tenant IDOR / Cross-tenant data exposure" findings auto-flagged by security-guidance plugin on the invoices + expenses list pages (Opus turn 4 + 6). Suggested fix (`where: { tenantId: ... }`) DOES NOT APPLY at the schema level: Invoice (schema.prisma:639-675), Customer (336-372), Expense (1854-1878), ExpenseCategory (1837-1851), Payment (677-695) all lack a tenantId column. The existing `crm/customers/page.tsx` — explicit pattern reference endorsed by user — has identical exposure. Single-tenant dev environment ("demo") = not currently exploitable. User acknowledged and chose option (a): log Direction K, proceed to dashboard.

TOKEN_ESTIMATE: ~110K Opus 4.7 architect session (3 Sonnet dispatches at 170-173K subagent tokens / 6-8 tool uses each; ~25 Opus Bash + ctx_batch_execute invocations for scout/verify/commit/push/playwright; 2 Opus Writes (this STATE.md + auto-memory project_orqafy_p1_session_2026-05-31.md); 1 Opus Edit (auto-memory MEMORY.md index). All Sonnet dispatches stayed within V32 R2 ≤500-line cap + V32.1 per-dispatch budget; no thrash.

CHECKPOINT_TYPE: full
TIER_CLASSIFICATION: T2 Moderate per memory-governance §1 — total scope ~265 lines across 6 files: docker-compose.app.hot.yml NEW (45L) + COMMANDS.md +16L + clients/page.tsx -12+10 + clients/loading.tsx -9 (deleted) + clients/error.tsx -22 (deleted) + app-sidebar.tsx -2 + settings/page.tsx -5+146 = 4 modify + 2 delete + 1 new. Split across 3 Sonnet dispatches by surface boundary (dev infra / clients retirement / settings landing) per V32.1 operational note.

FILES_TOUCHED (this session):
  Commit 8ac919b feat(dev): hot-reload compose variant + clients→crm/customers redirect:
    deploy/compose/dev/docker-compose.app.hot.yml                                NEW 45L
    COMMANDS.md                                                                  +16L (Hot-Reload Dev Mode subsection)
    apps/web/src/app/(tenant)/[slug]/(app)/clients/page.tsx                      -12+10 (stub → redirect)
    apps/web/src/app/(tenant)/[slug]/(app)/clients/loading.tsx                   DELETED -9
    apps/web/src/app/(tenant)/[slug]/(app)/clients/error.tsx                     DELETED -22
    apps/web/src/components/layout/app-sidebar.tsx                               -2 (NAV_ITEMS line + Users import)

  Commit 82c0e48 feat(ui): real settings landing replaces stub:
    apps/web/src/app/(tenant)/[slug]/(app)/settings/page.tsx                     -5+146 (14L stub → 155L landing)

  Auto-memory writes (Opus permitted — user memory, not project files):
    /home/me/.claude/projects/.../memory/project_orqafy_p1_session_2026-05-31.md NEW
    /home/me/.claude/projects/.../memory/MEMORY.md                               +1 line index entry

  This STATE.md (Opus V32 R1 exception):
    .cline/STATE.md                                                              full rewrite

  Untracked verification artifact (gitignored per `dev-*.png` rule from 1557b2f):
    dev-settings-landing.png                                                     Playwright full-page screenshot of /demo/settings

DEV_VALIDATION_PROOF (this session):
  ✅ docker compose config -q on docker-compose.app.hot.yml: empty stdout (valid)
  ✅ Boot test: orqafy_dev_app_hot container up; pnpm install completed inside; next dev Ready in 1944ms; /api/health HTTP 200 in 44ms warm
  ✅ pnpm typecheck (web) after clients redirect: tsc --noEmit clean, 0 errors
  ✅ pnpm typecheck (web) after settings landing: 0 errors
  ✅ Playwright /demo/dashboard navigation list: 19 items, no "Clients"
  ✅ Playwright /demo/settings render: workspace card populated + 6 cards in correct order + Xendit as <link>, others as <generic>
  ✅ git log + git status: 2 commits (8ac919b + 82c0e48) on main, both pushed to origin/main, working tree clean
  ⏳ Lint not run — pre-existing `next lint` workspace-detection bug (picks /home/me/UbuntuDevFiles/1_COMPANY_DEV/CompanyApps/package-lock.json as root) blocks both lint runs; unrelated to this session's changes; `next lint` deprecated for Next 16 anyway

LAST_DONE:    Shipped invoices + expenses list pages (745413e + 4c36a7d), browser-verified both populated states via Playwright (3 invoices across 3 status values; 4 expenses across all 4 status values), acknowledged security findings + added Direction K-prime to BANK_FOR_LATER. Both commits on main, NOT pushed yet (user hasn't asked).

NEXT:         Continue P1:

              CONTINUE_P1 — dashboard is next. ~400L. Should reuse the formatCurrency + status-pill patterns established today. May want to split: KPI tiles (4-6 metrics — invoice outstanding, expense pending, etc.) + recent activity feed. Each is a single Sonnet dispatch worth.

              THEN demo stub (P1 6 of 6) — design pass required first, see WHAT_DID_NOT_GET_DONE.

              CONTINUE_DEV_DX — fix `next lint` workspace-detection bug (likely add outputFileTracingRoot to next.config.ts per the warning the run emitted, OR remove the stray /home/me/UbuntuDevFiles/1_COMPANY_DEV/CompanyApps/package-lock.json), OR migrate to direct ESLint CLI per Next 16 deprecation guidance.

              PIVOT_BACK_TO_DIRECTION_K — staging/Komodo migrations were deprioritized per [[feedback_dev_before_staging]]. P1 punch list is mid-flight (2/6 done) so pivoting now violates user feedback. NOT recommended.

              GOVERNANCE_BATCH — CHANGELOG_AI.md + lessons.md updates for this session (3 entries: hot-reload variant 🟢 change, clients→customers 🟤 decision, settings landing 🟢 change). One Sonnet dispatch. Could bundle with next session's first commit governance.

BANK_FOR_LATER (carried from prior STATE.md, still valid + 1 new entry):
  - CI workflow (ci.yml, NOT docker-publish.yml) still failing on `pnpm tools:check-env` because .env.dev is gitignored and never on the runner. Pre-existing gap.
  - Turnstile prod keys still pending — currently using Cloudflare test keys (always pass). Required before public traffic.
  - apps/worker/src/__tests__/tenant-provisioning.test.ts still skipped — can be reactivated now that Direction J landed.
  - Direction L module slice still queued. Direction K (Komodo staging migrations) remains DEPRIORITIZED per dev-first feedback.
  - **NEW: Direction K-prime — Invoicing-surface tenantId parity.** Add tenantId to Invoice, Payment, Customer, Expense, ExpenseCategory, Subscription. Each needs: schema field + index + relation back from Tenant + backfill migration (single-tenant dev → all rows pinned to demo tenant id) + L6 tenant-guard coverage on these models + add `where: { tenantId }` filter to invoices/expenses/customers list pages + helper functions (loadInvoiceForTenant analogous to loadPoForTenant from Batch 29) for byId/update paths in invoice + expense routers. Pattern: JOIN-backfill cascade where applicable (Payment via Invoice, CustomerCreditAccount via Customer). Closes the security-review findings raised by the auto-review on commits 745413e + 4c36a7d. Sister to original Direction K (Komodo staging) which is also deprioritized — pick the name "Direction K-prime" or just "Direction K (invoicing-surface)" when shipping. Estimated: T2-T3, multi-Sonnet-dispatch, dedicated session.
  - Workflow concurrency group `docker-${{ github.ref }}` with `cancel-in-progress: true` means rapid-succession pushes cancel earlier runs. Not a problem with squash-merge practice.
  - NEW: `next lint` workspace-root detection bug — Next picks `/home/me/UbuntuDevFiles/1_COMPANY_DEV/CompanyApps/package-lock.json` instead of project's pnpm-lock.yaml. Either pin via outputFileTracingRoot in next.config.ts, delete the stray parent package-lock.json, or migrate to direct ESLint CLI (Next 16 deprecation path).

BLOCKERS:     none

GIT_BRANCH:   main (in sync with origin — both this-session commits 8ac919b + 82c0e48 pushed)

PORTS:        (unchanged from prior session — see .env.dev). Currently bound:
              - orqafy_dev_app_hot (HOT MODE): 0.0.0.0:42951->42951/tcp — LIVE, healthy, next dev ready
              - orqafy_dev_postgres / valkey / pgbouncer / minio / mailhog / pgadmin / worker: backing services still up from earlier in day
              - orqafy_dev_app (regular): DOWN (removed via compose down during boot test) — to restore: `docker compose --env-file .env.dev -f deploy/compose/dev/docker-compose.app.hot.yml down` then `bash deploy/compose/start.sh dev up -d`

MODELS:
  planning:   claude-code (V31 primary)
  execution:  claude-sonnet-4-6 via Claude Code (V32 R1 — Zero Opus Execution; all file writes through Sonnet except this STATE.md + auto-memory files which are user-memory not project files)
  governance: gemini-2.5-flash-lite (cheapest, non-critical writes; deferred for this session — bundle in next session's first governance batch if desired)

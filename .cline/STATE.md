# Project State — Orqafy
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-05-31 01:25 GMT+8 by CLAUDE_CODE Opus 4.7 (Tasks #3 + #4 + worker-build gap closed; docker-publish hardened; 3 commits pushed; governance batched)

PHASE:        ✅ DEFERRED TASKS #3 + #4 + WORKER-BUILD GAP SHIPPED — docker-publish.yml now builds and pushes BOTH web (orqafy:staging-latest) AND worker (orqafy-worker:staging-latest) images to Docker Hub. Build hardened: arm64 dropped (Komodo VPS is amd64-only; arm64 emulation via QEMU wasted 70% of wall-clock for a platform nothing pulls), timeout raised 30→60m as belt-and-braces. Three commits shipped main: 6df0324 (feat: build worker image alongside web), 08a1a09 (fix: drop arm64, raise timeout), ea024c8 (governance). Komodo staging Stack has auto_update: true so it auto-pulls both digests on next poll — staging app + worker should come alive without manual intervention.

HANDOFF:      none (clean close). Opus session executed entirely through V32 R1 Zero Opus Execution discipline — no direct file writes except this STATE.md. Three Sonnet dispatches: (a) feat dispatch added worker build to docker-publish.yml + WORKER_IMAGE_NAME to .env.staging/.env.prod/.env.example, (b) fix dispatch dropped arm64 platforms + raised timeout, (c) governance dispatch appended CHANGELOG_AI + lessons + .whatsnext updates. Two background workflow watches: 26687368923 CANCELLED (30m timeout — surfaced arm64 cost), 26689984984 SUCCESS in 4m 47s post-fix.

DOCKER_PUBLISH_HARDENED:
              Workflow .github/workflows/docker-publish.yml now: (1) builds both apps/web/Dockerfile (→ ${DOCKERHUB_USERNAME}/orqafy:tagSet) AND apps/worker/Dockerfile (→ ${DOCKERHUB_USERNAME}/orqafy-worker:tagSet) in a single sequential job, (2) uses per-image GHA cache scopes (scope=web / scope=worker) to prevent cache cross-pollination, (3) builds linux/amd64 only (no arm64), (4) job timeout-minutes: 60. Tag set per image: :latest + :staging-latest + :sha-<short> + :main (last from type=ref event=branch on main). WEB_IMAGE_NAME + WORKER_IMAGE_NAME both inlined as env block at top of workflow YAML — no GH Actions variables needed (workflow hardcodes the image suffixes orqafy / orqafy-worker).

WORKFLOW_RUN_TIMING (banked to lessons.md as 🟤 decision):
              First run post-feat-commit (26687368923, amd64+arm64 multi-platform): web 24m 59s + worker 4m 46s before 30m job timeout cancelled the run. WEB image did push despite the cancellation. Second run post-fix-commit (26689984984, amd64-only): web 2m 26s (cache-warm from prior amd64 layers) + worker 1m 55s (cold cache amd64-only). 6× speedup. Decision: amd64-only by default; add `linux/arm64` per-image only when a real consumer emerges (e.g. M-series Mac dev workflow added to PRODUCT.md, or Komodo migrates to ARM nodes). Single-line revert per build-push-action step.

TOKEN_ESTIMATE: ~80K Opus 4.7 architect session — 3 Sonnet dispatches (feat: ~177K subagent tokens / 17 tool uses; fix: ~169K / 10 tool uses; governance: ~185K / 13 tool uses) + ~12 Opus Bash invocations (gh CLI: auth/secret/variable/run list, git log/show/status, file-size verifications, env grep) + 2 Opus background watchers (gh run watch on workflow IDs 26687368923 + 26689984984) + 3 Opus Reads (STATE.md resume, docker-publish.yml verification, deferred-tasks memory) + 1 Opus Write (this STATE.md, the V32 R1 exception). All Sonnet dispatches stayed within V32 R2 (≤500 lines per task) and V32.1 operational guideline (per-dispatch tool-use budget); no thrash.

CHECKPOINT_TYPE: full
TIER_CLASSIFICATION: T1 Lightweight per memory-governance §1 — total scope across both feat + fix commits ~35 lines touched in 4 files (.github/workflows/docker-publish.yml 61→85 lines net, .env.staging +1 line, .env.prod +1 line, .env.example +1 line). No single file > 300 lines edited. No scout-then-edit needed.

FILES_TOUCHED (this session):
  Commit 6df0324 feat(ci): build worker image alongside web in docker-publish:
    .github/workflows/docker-publish.yml                                        (61 → 85 lines: IMAGE_NAME → WEB_IMAGE_NAME + added WORKER_IMAGE_NAME; added 2 sequential metadata + build-push steps for worker; per-image cache scopes)
    .env.example                                                                (+1 line: WORKER_IMAGE_NAME=orqafy-worker, line 76)
    .env.staging                                                                (gitignored; +1 line: WORKER_IMAGE_NAME=orqafy-worker after IMAGE_NAME line 13)
    .env.prod                                                                   (gitignored; +1 line: WORKER_IMAGE_NAME=orqafy-worker after IMAGE_NAME line 14)

  Commit 08a1a09 fix(ci): drop arm64 from docker-publish, raise timeout to 60m:
    .github/workflows/docker-publish.yml                                        (3 line edits: timeout-minutes 30→60; both platforms lines linux/amd64,linux/arm64 → linux/amd64)

  Commit ea024c8 chore(governance): close Tasks #3 + #4 — docker-publish hardened:
    docs/CHANGELOG_AI.md                                                        (new entry at top: 2026-05-31 docker-publish hardened)
    .cline/memory/lessons.md                                                    (new 🟤 decision entry: docker-publish.yml builds amd64-only by default)
    .whatsnext                                                                  (banner updated to Direction K UNBLOCKED; Direction K section DEFERRED→UNBLOCKED; Tasks #3/#4 line items removed)

  Uncommitted (this STATE.md write, V32 R1 Opus exception):
    .cline/STATE.md                                                             (full rewrite, this — to be committed in next governance batch or by next session resume)

DEV_VALIDATION_PROOF (this session):
  ✅ Workflow run 26689984984 (commit 08a1a09): conclusion=success, 4m 47s
     - Build and push (web) amd64: success 2m 26s (15:14:13 → 15:16:39 UTC, cache-warm)
     - Build and push (worker) amd64: success 1m 55s (15:16:40 → 15:18:35 UTC, cold cache)
  ✅ git log --oneline -4: 3 expected commits on main + prior STATE.md checkpoint
  ✅ git status -sb: "## main...origin/main" (no divergence — all 3 commits pushed)
  ⏳ Komodo staging auto-pull: NOT VERIFIED this session (next session can confirm via staging URL or Komodo UI)

LAST_DONE:    Tasks #3 + #4 + worker-build gap closed. Three commits pushed to origin/main (range bd6950f..ea024c8). Workflow run 26689984984 SUCCESS — both orqafy:staging-latest AND orqafy-worker:staging-latest live on Docker Hub. Sonnet wrote all code/config/governance edits; Opus only wrote this STATE.md (V32 R1 compliant).

NEXT:         **Direction K — 12 staging/prod migration gates.** Was blocked on Tasks #11 (Direction J: @orqafy/db CJS dist, shipped earlier today) + #3 + #4 (this session). All three now closed; Direction K is the natural top of queue. Recommended approach for next session: (a) verify Komodo staging is actually live by visiting the staging domain from .env.staging APP_DOMAIN (or pinging /api/health), (b) if staging is reachable, begin scoping gate 1 of 12 from the original Direction K plan (likely lives in docs/IMPLEMENTATION_MAP.md or .whatsnext — check there first), (c) if staging is NOT reachable, triage why (Komodo poll interval, Komodo image-pull credentials, Komodo Stack config) before touching migration gate work.

BANK_FOR_LATER (not blocking, surfaced this session OR carried over):
  - CI workflow (ci.yml, NOT docker-publish.yml) still failing on `pnpm tools:check-env` because .env.dev is gitignored and never on the runner. Pre-existing gap, NOT caused by Tasks #3/#4. Three fix options documented in prior STATE.md. T1.
  - Turnstile prod keys still pending — currently using Cloudflare test keys (always pass). Required before public traffic.
  - apps/worker/src/__tests__/tenant-provisioning.test.ts still skipped — was disabled while @orqafy/db couldn't be loaded; can be reactivated now that Direction J landed.
  - Direction L module slice (catalog / CRM / HR / accounting / POS) still queued behind Direction K per Batch 32 STRATEGIC FINDING.
  - Workflow concurrency group `docker-${{ github.ref }}` with `cancel-in-progress: true` means a second push to main mid-build cancels the first run. If you push multiple commits in quick succession, only the latest survives. Not a problem if commits are coalesced via squash-merge (current Rule 23 practice).

BLOCKERS:     none

GIT_BRANCH:   main (in sync with origin)

PORTS:        (unchanged from prior session — see .env.dev)

MODELS:
  planning:   claude-code (V31 primary)
  execution:  claude-sonnet-4-6 via Claude Code (V32 R1 — Zero Opus Execution; all file writes through Sonnet except this STATE.md)
  governance: gemini-2.5-flash-lite (cheapest, non-critical writes)

# Project State — Orqafy
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-22 00:00 GMT+8 by CLAUDE_CODE Opus 4.7 (Batch 21b CLOSE — TenantXenditConfig CRUD + admin UI shipped, isVerified verification loop wired end-to-end)

PHASE:        ✅ Phase 8 Batch 21b COMPLETE — Direction F prerequisite (split 2 of 3) SHIPPED. Tenant Administrators can now save/test/delete their own Xendit credentials at /[slug]/settings/xendit. NEW apps/web/src/server/trpc/routers/admin-xendit-config.ts (153 lines, 4 procedures: get/upsert/testConnection/delete) + admin UI (page.tsx 22 lines + config-form.tsx 275 lines, masked-display + isVerified badge + Test connection button + rotate-credentials form requiring all 3 secrets re-entered on every save) + 8 unit tests (encryption-roundtrip, masked-fields, isVerified-reset, testConnection happy/failure, delete-FK-block, FORBIDDEN on non-admin, cross-tenant isolation). _app.ts registers `adminXenditConfig: adminXenditConfigRouter`. Squash-merged as ff61bea. 714/714 GREEN (was 706, +8). 23→24 test files. Web typecheck + lint clean. ZERO schema changes (TenantXenditConfig already in init migration at schema.prisma:160). Rule 23 maintained — single discrete branch (feat/batch-21b-xendit-config-admin), squash-merged + -D deleted.
HANDOFF:      none (clean close — Batch 21c queued: refactor getXenditClient(tenantId) + webhook tenant resolution to consume TenantXenditConfig per-tenant. Direction F COMPLETE upon 21c ship.)
TOKEN_ESTIMATE: ~45K single Opus 4.7 session — pre-flight (9-point with 3 follow-up batches resolving ADMIN_ROLES content + Xendit expireInvoice SDK confirmation + vitest command) + 8 RED tests + router with 4 procedures + page shell + 275-line client form + 2 typecheck/lint fix iterations + governance close. Well within 80K SAFE zone, no /clear, no thrash. Tier 2 Moderate per §1 (6 files = 5 NEW + 1 MOD, 3 modules, depth 2).
CHECKPOINT_TYPE: full
TIER_CLASSIFICATION: T2 Moderate (5 NEW + 1 MOD = 6 files; modules: server router + app UI + tests; depth 2 — consumes @/lib/crypto + xendit-node SDK). RED→GREEN explicit: vitest confirmed file-level RED ("Cannot find module .../admin-xendit-config"), implemented router + registered → 8/8 GREEN first run, no test fix iteration. Two typecheck/lint fixes after (architectural surprise on EcommerceOrder tenant-schema isolation + dead type assertion), zero test fixes.

FILES_TOUCHED (Batch 21b, squash-merged as ff61bea):
  - apps/web/src/server/trpc/routers/admin-xendit-config.ts (NEW, 153 lines — 4 procedures with explicit Rule 7 multi-tenant scoping; uses ctx.tenantId on all public-schema TenantXenditConfig queries; uses tenant-schema search_path implicit scoping on EcommerceOrder count query)
  - apps/web/src/__tests__/admin-xendit-config.test.ts (NEW, 273 lines — 8 tests with vi.mock @orqafy/db + @/server/lib/rate-limit + xendit-node module; sets APP_ENCRYPTION_KEY at file-top for lazy-read consumers)
  - apps/web/src/app/(tenant)/[slug]/(app)/settings/xendit/page.tsx (NEW, 22 lines — Server shell with metadata + intro copy)
  - apps/web/src/app/(tenant)/[slug]/(app)/settings/xendit/config-form.tsx (NEW, 275 lines — Client form: hydrate-via-useEffect for non-sensitive fields, sensitive fields always empty, status panel with isVerified badge, Test connection button gated to unverified-existing-config, rotate-credentials form, delete with window.confirm)
  - apps/web/src/server/trpc/routers/_app.ts (+2 lines: import + register adminXenditConfigRouter)

DIRECTION_F_PROGRESS:
  ✅ 21a Encryption infrastructure (crypto.ts + APP_ENCRYPTION_KEY + 9 tests) — shipped 2026-05-21 (976df52)
  ✅ 21b TenantXenditConfig CRUD + admin UI (this batch) — shipped 2026-05-22 (ff61bea)
  ⬜ 21c NEXT: Refactor getXenditClient(tenantId) to read TenantXenditConfig + decrypt. Refactor webhooks/route.ts to identify tenant via xenditPaymentId lookup BEFORE signature verify (EcommerceOrder.xenditPaymentId → ctx → TenantXenditConfig.webhookTokenEnc → decrypt → timingSafeEqual). Refactor storefront.placeOrderAsCustomer's xendit branch to use per-tenant secret. Direction F COMPLETE upon 21c ship. Estimated ~20K T2.

BATCH_21b_PROCESS_NOTES:
  - vercel-plugin auto-suggestion hooks fired 4× this batch (next-forge on test file write, nextjs + next-cache-components on page.tsx write, react-best-practices on config-form.tsx write). ALL correctly skipped per Rule 28 priority order + SessionStart instruction. 10th consecutive batch this pattern holds. NET POSITIVE finding: even when the hook content is skipped, the trigger itself functions as a mini-checkpoint reminder — react-best-practices hook caught a genuine setState-during-render anti-pattern that I self-corrected to useEffect BEFORE running quality gates. The prompt-to-self-review value is independent of whether the skill is loaded.
  - 9-POINT PRE-FLIGHT RESOLVED 3 GAPS via 2 follow-up ctx_batch_execute calls: (1) ADMIN_ROLES Set is `["Administrator", "Platform Owner"]` (NOT "Tenant Administrator" as .whatsnext implied — naming drift now corrected in router + form copy). (2) xendit-node v7 SDK has `Invoice.expireInvoice({invoiceId})` returning Promise<Invoice> — confirmed via grep on the actual SDK .d.ts under node_modules/.pnpm/. (3) Test command is `pnpm --filter @orqafy/web test` (root has `turbo run test`, app has `vitest run` aliased as `test`).
  - PRE-FLIGHT MISCONCEPTION caught at typecheck: assumed EcommerceOrder had a tenantId column for explicit scoping. Reality: EcommerceOrder is a tenant-schema-per-tenant entity (no tenantId column — search_path isolation). Confirmed via schema.prisma model definition at line 1974 + grep on existing storefront.ts queries (6 places query ecommerceOrder WITHOUT explicit tenantId). Removed the dead WHERE clause + added inline comment. Lesson banked: pre-flight should grep `@@schema("public")` declarations for each model in scope to determine whether tenantId is a queryable column.
  - DESIGN DECISION — MASKED-DISPLAY: never return *Enc fields to client. Return only publicKey/isLive/isVerified/enabledMethods + boolean has-value flags. Form sensitive inputs always start empty. User must re-enter ALL THREE secrets on every save. Trade-off: no partial-update path (changing only isLive requires full re-enter). Acceptable for v1; partial-update via optional Zod inputs deferred.
  - DESIGN DECISION — TESTCONNECTION INSTANTIATES SDK DIRECTLY in 21b: `new Xendit({secretKey: decrypt(row.secretKeyEnc)})` instead of via @/lib/xendit.getXenditClient(). Lets 21c refactor getXenditClient(tenantId) without touching 21b code. Mock pattern: vi.mock("xendit-node", ...) at module level + vi.hoisted for the mock fns.
  - DESIGN DECISION — XENDIT TEST INVOICE AMOUNT: IDR 10,000 (Xendit's typical floor for test invoices). externalId pattern: `test-${ctx.tenantId}-${Date.now()}`. Invoice duration: 60 seconds. Then immediately expire via Invoice.expireInvoice({invoiceId}). On success, flip isVerified=true via tenantXenditConfig.update. On failure, error propagates — isVerified stays false (no update call with `isVerified: true`).
  - 🔴 CRITICAL — STAGING/PROD KEY DEPLOYMENT (same flag as 21a): Before 21c ships OR before this CRUD is used on staging/prod, human MUST generate APP_ENCRYPTION_KEY for each environment (different per env — never reuse) and add to .env.staging + .env.prod. Without this, env.ts validation succeeds at boot BUT the first upsert call throws "APP_ENCRYPTION_KEY is not configured" from getKey() at decrypt-time. Flag remains open until keys are manually deployed.

PRIOR BATCH 21a baseline 706/706 GREEN → Batch 21b 714/714 GREEN. Test files: 24 (was 23, +1 admin-xendit-config.test.ts). Cumulative test delta from Batch 14 baseline (648) to now (714): +66 tests across 9 batches (3 cancel-release + 5 fulfillment + 5 createXenditInvoice + 8 webhook + 16 cart-reducer + 7 placeOrderAsCustomer + 5 Xendit-for-guests + 9 crypto + 8 admin-xendit-config).

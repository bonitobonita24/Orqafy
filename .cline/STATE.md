# Project State — Orqafy
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-17 18:45 GMT+8 by CLAUDE_CODE Opus 4.7 (Batch 18 Item 1 CLOSE — cart client state shipped)

PHASE:        ✅ Phase 8 Batch 18 Item 1 COMPLETE — Direction A continuation (E-commerce customer self-service Phase 1 / Cart). Single item shipped: pure cart reducer (cart-reducer.ts, 16 unit tests TDD-driven RED→GREEN) + Client Provider with per-tenant localStorage sync (cart-store.tsx) + AddToCartButton client component + CartDrawer using shadcn Sheet + NEW store layout (apps/web/src/app/(tenant)/[slug]/store/layout.tsx) wrapping store routes with CartProvider + sticky header containing CartDrawer trigger. Product detail page mount-point updated — disabled "Place order (coming soon)" placeholder replaced with <AddToCartButton>. Squash-merged as 03f389e. 685/685 GREEN (was 669 +16 cart-reducer tests). Web typecheck + lint clean. ZERO migrations (pure client state — schema pre-flight 5× consecutive). Rule 23 maintained — single discrete branch (feat/batch-18-item-1-cart-state), squash-merged + -D deleted.
HANDOFF:      none (clean close — Batch 18 Item 2 next: checkout page + placeOrderAsCustomer publicProcedure, ~25K T2-T3, own branch feat/batch-18-item-2-checkout-public-procedure)
TOKEN_ESTIMATE: ~18K single Opus 4.7 session — reducer + provider + 2 components + layout + page mount + tests + governance close. No /clear or thrash. Comfortably within 80K SAFE zone.
CHECKPOINT_TYPE: full
TIER_CLASSIFICATION: T2 (8 files: 6 NEW + 1 MODIFIED + 1 shadcn-generated = under the 12-file threshold; 2 modules — components/cart + app/store; no DB; no auth surface change). RED→GREEN explicit: 16 reducer tests written first, vitest confirmed all 16 fail with "Failed to resolve import @/lib/cart-reducer", then implementation made them GREEN.

FILES_TOUCHED (Batch 18 Item 1, squash-merged as 03f389e):
  - apps/web/src/lib/cart-reducer.ts (NEW, 79 lines: pure reducer + types + selectors)
  - apps/web/src/lib/cart-store.tsx (NEW, 140 lines: CartProvider + useCart hook + localStorage sync)
  - apps/web/src/components/cart/add-to-cart-button.tsx (NEW, 62 lines: client component, qty input + add + toast)
  - apps/web/src/components/cart/cart-drawer.tsx (NEW, 136 lines: shadcn Sheet drawer with line items + subtotal + disabled checkout placeholder)
  - apps/web/src/app/(tenant)/[slug]/store/layout.tsx (NEW, 36 lines: shared layout with CartProvider + sticky header)
  - apps/web/src/components/ui/sheet.tsx (NEW via shadcn CLI, 140 lines)
  - apps/web/src/__tests__/cart-reducer.test.ts (NEW, 167 lines: 16 unit tests covering all actions + selectors + immutability)
  - apps/web/src/app/(tenant)/[slug]/store/products/[id]/page.tsx (+8/-9: replaced placeholder with AddToCartButton)

BATCH_18_PLAN (Item 1 SHIPPED 2026-05-17 18:45):
  ✅ Item 1: Cart client state + AddToCartButton + CartDrawer + store layout (03f389e)
  ⬜ Item 2 (DEFERRED to next batch): Checkout page /[slug]/store/checkout (Server shell + Client form) + storefront.placeOrderAsCustomer publicProcedure (FIRST publicProcedure on storefront — requires rate-limit + guest-vs-session-auth focus) — branch: feat/batch-18-item-2-checkout-public-procedure
  (Deferred to Batch 19+: Direction B Mobile Expo scaffold, Direction C Xendit production-readiness async webhook+pino, Direction D carry-forward bundle accounting.reverse $transaction + AUTH_TRUST_HOST env)

BATCH_18_ITEM_1_PROCESS_NOTES:
  - vercel-plugin auto-suggestion hooks fired several times (shadcn on npx shadcn add, next-cache-components + nextjs on store/layout.tsx Write, next-forge + react-best-practices on sheet.tsx Read). All correctly skipped per Rule 28 priority order + SessionStart instruction + Batch 17 lesson — confirmed 5th consecutive batch this pattern holds. NEVER auto-load vercel skills on routine edits.
  - Decided to SPLIT Direction A into two batches rather than ship both Items in one session. First intentional scope-trim of the batch series. Rationale: Item 2 introduces the first publicProcedure on storefront router, which deserves dedicated rate-limit (rateLimiters.public 30/min) verification + a careful decision on guest-vs-required-login flow. Bundling with cart UI scaffold would have rushed both.
  - RED→GREEN discipline applied EXPLICITLY: wrote 16 cart-reducer tests first → ran vitest → confirmed import-resolution failure (all 16 RED) → implemented cart-reducer.ts → re-ran → 16/16 GREEN. Cheaper than batched verification.
  - Pure-reducer-extracted-from-Provider pattern: cart-reducer.ts is pure TypeScript (no "use client", no React), cart-store.tsx is the "use client" Provider that imports it. Reducer gets full unit coverage in node environment. Provider stays uncovered (consistent with other UI-only components in this project — PayWithXendit, OrderStatusActions, FulfillmentForm all shipped without component tests).
  - Per-tenant localStorage key `orqafy-cart-${slug}` prevents cart bleed between tenants on a shared device. Hydration flag in Provider prevents both SSR mismatch and the pre-mount click loss on AddToCartButton.
  - Lint surfaced 2 errors caught at gate stage: stale @next/next/no-img-element disable comment (2nd occurrence — Batch 15 Item 3 was first; remembering this for permanent retention) and an unnecessary type assertion after structural narrowing. Both fixed inline with single-line edits.

PRIOR BATCH 17 (Xendit checkout end-to-end, see CHANGELOG_AI for detail) baseline 669/669 GREEN → this batch 685/685 GREEN. Test files: 21 → 22 (+1 cart-reducer.test.ts). Cumulative test delta from Batch 14 baseline (648) to now (685): +37 tests across 5 batches (3 cancel-release + 5 fulfillment + 5 createXenditInvoice + 8 webhook + 16 cart-reducer).

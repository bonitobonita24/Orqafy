# Project State — Orqafy
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-05-29 GMT+8 by CLAUDE_CODE Opus 4.7 (Batch 28 CLOSE — Direction I-3 COMPLETE: PurchaseOrderItemAllocation.tenantId parity — grandchild JOIN-backfill from PurchaseOrderItem.tenant_id on item_id)

PHASE:        ✅ Phase 8 Batch 28 COMPLETE — Direction I-3 SHIPPED end-to-end. Four Sonnet 4.6 dispatches under V32 Zero Opus Execution: I-3-1 RED test (1 assertion on tx.purchaseOrderItemAllocation.create data.tenantId — commit 37592c6; verified RED via vitest -t "Direction I-3" before commit; spy received { itemId, type, quantity } with NO tenantId — clean RED). I-3-2 schema + migration (PurchaseOrderItemAllocation.tenantId field + tenant relation + @@index([tenantId]); Tenant.purchaseOrderItemAllocations back-pointer; migration 20260529120000_add_tenant_id_to_purchase_order_item_allocations using 5-step JOIN-backfill from purchase_order_items ON item_id — Batch 27 pattern reapplied one level deeper since PurchaseOrderItem.tenantId is NOT NULL from Batch 27; commit cb6eab5; prisma client regenerated clean 0 errors). I-3-3 router GREEN (1 surgical edit at purchasing.ts L365: `tenantId: ctx.tenantId,` injected into tx.purchaseOrderItemAllocation.create data — minimal-diff principle, identical shape to Batch 27 I-2-3; commit 5ee237d; 749/749 GREEN, typecheck 0 errors, lint 0 errors). I-3-4 governance close (CHANGELOG_AI.md Batch 28 entry appended after Batch 27 newest-at-bottom; IMPLEMENTATION_MAP.md Last-updated header rewritten to 749 GREEN + Batch 28 ✅ COMPLETE marker; .whatsnext 4 surgical replaces — title → Batch 28 COMPLETE, deploy gates 6→7 with new migration 28 row, I-3 entry marked ✅ COMPLETE, I-4 promoted to ★ recommended next; lessons.md 1 typed entry — 🟢 three-generation tenant chain CLOSED via JOIN-backfill cascade proven at 3 generations deep; commit fba367d). Squash-merge to main complete: commit c10ca11. Feature branch feat/purchasing-poia-tenant-i3 deleted.

HANDOFF:      none (clean close — Direction I-3 COMPLETE). PRE-FLIGHT FINDING confirmed: PurchaseOrderItem.tenantId from Batch 27 was a clean parent backfill source — JOIN UPDATE from purchase_order_items produced 0 NULL rows (FK guarantees every allocation has parent item, and Batch 27 made all item.tenantId NOT NULL). Scope guard confirmed: tx.purchaseOrderItemAllocation.create has exactly ONE call site at purchasing.ts L363 (now L365) inside po.create transaction. Two UPDATE sites at L637/L659 are by-id processedAt updates from goodsReceipt flow — they DON'T set tenantId (column is set at create time, never overwritten), deliberately OUT of scope per grandchild-deferral discipline. 🟢 PATTERN PROVEN AT 3 GENERATIONS DEEP: JOIN-backfill cascade is now canonical for any arbitrarily-deep parent-scoped tenancy backfill. PO (Batch 26) → POI (Batch 27) → POIA (Batch 28) chain CLOSED. Direction I-5 sibling models all have PurchaseOrder as parent (1-level deep JOIN) or GoodsReceipt as parent (2-level deep — GR→PO JOIN cascade) — every remaining purchasing model achievable in tier-1 sub-batch. SEVEN DEPLOY GATES on staging/prod: (1) APP_ENCRYPTION_KEY in .env.staging + .env.prod + Komodo restart, (2) prisma migrate deploy of 21c (ecommerce_orders.tenantId), (3) prisma migrate deploy of 22 (webhookProcessedAt), (4) prisma migrate deploy of 24 (ecommerce_order_items.tenantId), (5) prisma migrate deploy of 26 (purchase_orders.tenantId), (6) prisma migrate deploy of 27 (purchase_order_items.tenantId), (7) NEW prisma migrate deploy of 28 (purchase_order_item_allocations.tenantId). See docs/deployment-direction-f.md (gates 6+7 may need follow-on doc-only batch addendum if not yet documented before next deploy).

TOKEN_ESTIMATE: ~32K Opus 4.7 architect-only session — pre-flight 2x ctx_batch_execute scout (initial scope + RED template + schema/router anchors) + BUILD BATCH PROPOSAL with verified Tier 1 classification + 4 Sonnet 4.6 dispatches (I-3-1, I-3-2, I-3-3, I-3-4) + 1 governance-anchor ctx scout before I-3-4 + STATE.md self-write (Opus exception V32 R1) + bash squash-merge. No thrash. No /clear. No re-decomposition. No path-fix retry (Batch 26 mount-key drift lesson applied — Sonnet prompts pinned `purchasing.po.*` from the start; Batch 27 I-2 template cloned for I-3 with single-symbol substitution). Sonnet sub-task token totals: I-3-1 ~166K (3 tool uses) + I-3-2 ~171K (11 tool uses) + I-3-3 ~166K (3 tool uses) + I-3-4 ~184K (16 tool uses) = ~687K subagent tokens total across executor dispatches.

CHECKPOINT_TYPE: full
TIER_CLASSIFICATION: T1 Lightweight per memory-governance §1 — 4 build files / 1 module (purchasing) / +99 -10 net lines (8 files changed in squash). Matches BUILD BATCH PROPOSAL Option A estimate (~7K direct + governance pass). V32 R3 line ranges used throughout: schema.prisma (2138L → 2 targeted blocks: Tenant @ purchaseOrderItems back-pointer line, PurchaseOrderItemAllocation @ L804-821), purchasing.ts (709L → 1 surgical edit at L365), purchasing.test.ts (809L → 843L append-only — 34 new lines). V32 R5 Sonnet Scout applied via 2 ctx_batch_execute scout batches before any dispatch.

FILES_TOUCHED (Batch 28, 4 commits squash-merged as c10ca11):
  Build commits (3) on branch feat/purchasing-poia-tenant-i3:
    37592c6 (I-3-1 RED test):
      - apps/web/src/__tests__/purchasing.test.ts (+34 lines: 1 RED test in new describe block `purchasing.po — Direction I-3 PurchaseOrderItemAllocation tenantId scoping (RED)`; assertion on tx.purchaseOrderItemAllocation.create receiving data.tenantId === ctx.tenantId; file 809 → 843 lines)
    cb6eab5 (I-3-2 schema + migration):
      - packages/db/prisma/schema.prisma (+3 lines: PurchaseOrderItemAllocation.tenantId + tenant relation + @@index([tenantId]); +1 line: Tenant.purchaseOrderItemAllocations back-pointer)
      - packages/db/prisma/migrations/20260529120000_add_tenant_id_to_purchase_order_item_allocations/migration.sql (NEW, ~24 lines — 5-step ADD-nullable → UPDATE FROM purchase_order_items JOIN on item_id → SET NOT NULL → FK ON DELETE RESTRICT ON UPDATE CASCADE → INDEX)
    5ee237d (I-3-3 router scoping — GREEN):
      - apps/web/src/server/trpc/routers/purchasing.ts (1 surgical edit at L365: `tenantId: ctx.tenantId,` inserted into tx.purchaseOrderItemAllocation.create data object)
  Governance commit (1):
    fba367d:
      - docs/CHANGELOG_AI.md (Batch 28 entry appended; newest-at-bottom convention preserved)
      - docs/IMPLEMENTATION_MAP.md (Last-updated header rewritten to 749 GREEN + Batch 28 ✅ COMPLETE marker)
      - .whatsnext (4 surgical replaces: title header → Batch 28 COMPLETE; deploy gates 6→7 with new migration 28 row; I-3 entry marked ✅ COMPLETE; I-4 promoted to ★ recommended next)
      - .cline/memory/lessons.md (+1 typed entry: 🟢 three-generation tenant chain CLOSED via JOIN-backfill cascade proven 3 generations deep)
  Squash-merge to main:
    c10ca11 (feat(purchasing): Direction I-3 — PurchaseOrderItemAllocation.tenantId parity (Batch 28))

DIRECTION_I_PROGRESS:
  ✅ I-1     PurchaseOrder RED tests + schema + router scoping — Batch 26 (28f5d9f)
  ✅ I-2     PurchaseOrderItem.tenantId parity — Batch 27 (a6aa114)
  ✅ I-3     PurchaseOrderItemAllocation.tenantId parity — Batch 28 (c10ca11 — THIS BATCH)
  ⬜ I-4     PurchaseOrder status-transition guards on update/submit/approve/markOrdered/cancel — ★ recommended next; Rule 25 RED tests first; loadPoForTenant helper banked (T1 ~6K)
  ⬜ I-5a    Sibling wave: ShippingCost + ShippingCostDistribution + GoodsReceipt (T2 ~12K)
  ⬜ I-5b    Sibling wave: GoodsReceiptItem + PurchaseInvoice (T2 ~10K)
  ⬜ Vendor  Q2 catalogue model; low blast radius; revisit after items/allocations/siblings/status-guards

PURCHASING_SURFACE_PROGRESS (post-Batch-28):
  ✅ PurchaseOrder                  — Batch 26 (tenantId column + scoped list/byId/create)
  ✅ PurchaseOrderItem              — Batch 27 (tenantId column + create-site injection; JOIN-backfill from PO)
  ✅ PurchaseOrderItemAllocation    — Batch 28 (tenantId column + create-site injection; JOIN-backfill from POI on item_id) — THIS BATCH
  ⬜ PurchaseOrder status guards    — queued as Direction I-4 ★ recommended next (RED tests first per Rule 25, T1 ~6K)
  ⬜ ShippingCost                   — queued as Direction I-5a
  ⬜ ShippingCostDistribution       — queued as Direction I-5a
  ⬜ GoodsReceipt                   — queued as Direction I-5a
  ⬜ GoodsReceiptItem               — queued as Direction I-5b
  ⬜ PurchaseInvoice                — queued as Direction I-5b
  ⬜ Vendor                         — Q2 catalogue, lowest priority

FULL_ECOMMERCE_ARC_PROGRESS (carried — unchanged this batch):
  ✅ 21a Encryption infrastructure (976df52)
  ✅ 21b TenantXenditConfig CRUD + admin UI (ff61bea)
  ✅ 21c Per-tenant Xendit consumer refactor + EcommerceOrder.tenantId (abe57ce)
  ✅ Batch 22 webhookProcessedAt
  ✅ Batch 23 Direction D — guest tracking + audit log + admin filters
  ✅ Batch 24 Direction G — EcommerceOrderItem.tenantId parity
  ✅ Batch 25 Direction H — pino logger + APP_ENCRYPTION_KEY startup health check + payment/webhook/admin log lines
  ✅ Batch 26 Direction I-narrow — PurchaseOrder.tenantId parity
  ✅ Batch 27 Direction I-2 — PurchaseOrderItem.tenantId parity
  ✅ Batch 28 Direction I-3 — PurchaseOrderItemAllocation.tenantId parity (this batch)

NEXT_RECOMMENDED: Direction I-4 (PurchaseOrder status-transition guards — add tenant-mismatch NOT_FOUND guards to update/submit/approve/markOrdered/cancel mutations at lines ~391-460). REQUIRES RED tests FIRST per Rule 25 TDD — 5 RED tests (one per mutation) asserting throw on tenantId mismatch, then 5 guard inserts using `loadPoForTenant(id, ctx)` helper pattern banked in Batch 26 security review (🟢 lesson 2026-05-29). T1 ~6K, expected single-Sonnet-dispatch per sub-task (I-4-1 RED tests, I-4-2 helper extract + apply, I-4-3 governance). Alternative: Direction I-5a (sibling wave — ShippingCost + ShippingCostDistribution + GoodsReceipt — T2 ~12K, may need 2 sub-batches). Defer Direction I-5b until I-4 + I-5a ship. Vendor remains lowest priority.

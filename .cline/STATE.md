# Project State — Orqafy
# Auto-generated by Claude Code. Never edit manually.
# Updated: 2026-07-12 by CLAUDE_CODE (RBAC §4 — role.delete completes custom-role CRUD, Full Auto)

PHASE:        RBAC §4 ROLLOUT — DONE. Un-gated [HOW] queue GENUINELY EXHAUSTED for this goal.
              The two pre-existing security gaps (D-RBAC-USERS-UNGATED, D-RBAC-PAYROLL-UNGATED)
              were owner-approved and CLOSED (cb0c783). This Full-Auto session closed the last
              un-gated completeness gap: role.delete now completes the §4 custom-role CRUD
              (0270086). All LOCAL on feat/tenant-rbac-3tier, HARD HOLD (unpushed). HEAD 0270086
              (+ a docs commit to follow). Remaining work is owner-gated [WHAT] only (see NEXT).
LAST_DONE:    1 code commit this Full-Auto session (after 496ec0d):
              • 0270086 — RBAC §4 role-builder: complete custom-role CRUD. The §4 matrix system
                had create/update/assign but NO delete (role-builder could create+edit custom
                roles but never remove one). Added role.delete (superAdminProcedure): TSA/PO only
                (assertCanManageRoles); 3 fixed system tiers immutable (assertSystemRoleImmutable);
                tenant-scoped NOT_FOUND; IN-USE GUARD blocks deletion (BAD_REQUEST) when any user
                is still assigned (no orphaned users); cascades rolePermission rows in a tx;
                audits DELETE. +5 router tests. UI: destructive "Delete role" two-step-confirm
                affordance (existing-role mode only). No schema/migration, no reseed.
              ── prior history (unchanged) ── cb0c783 (USERS+PAYROLL hardening) · git log
              75bc162..d7e1f5a for M5–M9 + RBAC §4 Tracks A/B/C (rollout 23/35 COMPLETE).
GATE (PM ground-truth, this session): web typecheck 0 · web vitest 1258/1258 (+5 new) · web
              eslint 0 · lint-design.sh --report-only PASS. Live Rule-16 Visual QA (dev :42951,
              demo tenant, Tenant Super Admin): happy-path delete removed "QA Temp Role"; in-use
              guard blocked deleting "Staff" (assigned users) with a 400 toast, role stayed;
              new-role mode shows no Delete button; Users/Billing rows disabled. Console: only
              favicon 404 (pre-existing) + the intended role.delete 400.
NEXT:         Owner-facing. This RBAC §4 goal is DONE and the un-gated [HOW] queue is exhausted.
              Remaining is owner-gated [WHAT] only:
              (1) Owner-gated (docs/PENDING_DECISIONS.md): reseed live/staging/prod, apply
                  migrations 20260710160000 + 20260711010000 to staging/prod, framework-sync
                  push, git-tag push (next = MINOR 0.11.0), staging/prod/demo deploy, D-PRIV-1
                  (RA 10173), D-NUM-1.
              (2) OPTIONAL polish (not required by the standard, does NOT hold the loop open):
                  a succession/assign UI (the two-way succession PROCEDURES + tests already
                  exist per §2, M2 — only a convenience UI is missing). role.delete is now DONE.
              ANTI-REGRESSION RULE (standing): the seed packages/db/src/seed/role-permissions.ts is
              the AUTHORITATIVE endpoint→action map. Matrix tests run under Platform-Owner BYPASS, so
              they do NOT catch mis-gating — PM must diff each endpoint→action against the seed + add
              a non-bypass allow/deny test. Do NOT re-audit IDOR (33/33, M8) or re-run P1/P2 (M5/6/7).
              Login (dev): admin@mail.com/admin (=Tenant Super Admin, demo) · user@mail.com/user
              (=Staff) · webmaster@orqafy.local (=Tenant Super Admin, pw in CREDENTIALS.md).
              App http://localhost:42951, tenant URL /demo/... Screenshots → screenshots/ (gitignored).
BLOCKERS:     none for [HOW] (queue GENUINELY EXHAUSTED for this RBAC goal). Owner-gated HARD HOLD.
GIT_BRANCH:   feat/tenant-rbac-3tier (unpushed — HARD HOLD). HEAD 0270086 (role.delete) + a
              docs commit on top; after v0.10.0 tag @ d2d6196. Prior: cb0c783, 496ec0d.
DEV STACK:    UP (db 42941 / redis 42943 / app 42951 hot-reload). Host-side DB: build DATABASE_URL
              from DB_USER/DB_PASSWORD/localhost:42941/DB_NAME (.env.dev uses ${..} Prisma won't
              resolve). psql: docker exec orqafy_dev_postgres.
OWNER-GATED (docs/PENDING_DECISIONS.md): D-MIG-APPLY · D-RBAC-C1..C4 (Wave C RBAC + reseed,
              ANSWERED) · D-PRIV-1 (RA 10173) · framework-sync push · git-tag push ·
              staging/prod/demo deploy · D-1/3/4 product scope · D-NUM-1.
              (D-RBAC-USERS-UNGATED + D-RBAC-PAYROLL-UNGATED RESOLVED 2026-07-12, commit cb0c783.)
MODELS:
  planning:   claude-opus (PM/architect)
  execution:  spec-executor (Sonnet) workers
  governance: claude-code

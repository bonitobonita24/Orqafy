# Project State — Orqafy
# Auto-generated by Claude Code. Never edit manually.
# Updated: 2026-07-11 by CLAUDE_CODE (Full-Auto M6 partial — dev-up + M6.1/M6.2 done, committed)

PHASE:        Full-Auto run (slot-4). M1..M5 ✅. M6 IN PROGRESS: M6.1 ✅ M6.2 ✅ (committed).
              NEXT (this-milestone remainder): M6.3 S-P1a rate-limiting · M6.4/5/6 Visual QA.
LAST_DONE:    M6 dev-up session — DEV STACK IS UP (deliberate rebuild off branch), clean fresh DB.
              • Brought up local dev stack (db+cache+storage+mailhog+app_hot hot-reload). Wiped stale
                postgres volume → fresh DB (removed accumulated QA cruft: t_qatest/retestco tenants,
                orphan t_demo schema). Ports: app 42951, db 42941.
              • M6.1 (D-RBAC-B-APPLY) ✅: applied migration 20260710160000_add_tenant_owner_flag to dev
                (is_tenant_owner col + one_tenant_owner_per_tenant partial index verified). Worker
                succession integration test 4/4 green against live DB.
              • M6.2 (S-P2a REMAINDER) ✅ — commit 5422479. Removed physical per-tenant schema machinery
                (createTenantSchema/tenantSchemaExists/dropTenantSchema from helper+barrel+worker+tests;
                kept toSchemaName). Canonical = single public schema + tenantId. This EXPOSED + FIXED
                two latent multi-tenant prod bugs the schema-per-tenant hack was masking:
                  (a) SEED wrote demo departments/expense-categories/warehouse via raw SQL into the
                      invisible t_demo schema (public.departments for demo = 0). Rewrote to Prisma
                      upserts into public. Removed the last SET search_path in the codebase.
                  (b) STALE single-column UNIQUE(code) indexes on warehouses/accounts/tax_rates/
                      expense_categories — the 20260616120000 migration used DROP CONSTRAINT (no-op on
                      a Prisma single-col @unique, which is a UNIQUE INDEX). Blocked 2 tenants ever
                      sharing a code (e.g. both provisioning 'vat-12'). NEW migration
                      20260711010000_drop_stale_single_col_code_uniques (correct DROP INDEX) — applied
                      to dev. ⚠ Staging/prod application = owner-gated HARD HOLD (see PENDING_DECISIONS).
                Verified: fresh seed → demo data in public (depts 9/wh 1/cats 9/accounts 35, 1 owner,
                zero t_* schemas); worker 15/15; web 1063/1063; typecheck+eslint+lint-design green.
              • Logged 2 global footgun lessons (~/.claude/LESSONS_GLOBAL.md): prisma DROP-CONSTRAINT
                no-op on single-col @unique; multitenant raw-insert-into-unread-physical-schema.
NEXT:         M6 remainder (fresh session; dev stack already UP — resume fast):
              1. M6.3 S-P1a rate-limiting. Scouted: middlewares exist in
                 apps/web/src/server/trpc/middleware/rate-limit.ts (withApiRateLimit/withAuthRateLimit/
                 withUploadRateLimit) but NOT composed into protectedProcedure; withAuthRateLimit unused.
                 authorize() is in apps/web/src/server/auth/config.ts (signature can take (creds,request)
                 → extract IP for rateLimiters.auth, tier=10/min). ⚠ trpc.ts importing withApiRateLimit
                 = CIRCULAR (middleware imports {middleware} from trpc.ts) → inline rateLimiters.api
                 from @/server/lib/rate-limit instead. ⚠ LOCK-OUT RISK: api tier=120/min/user; a
                 data-dense ERP fires many parallel queries per page → integration-test against the
                 RUNNING stack before committing; consider mutation-only or higher ceiling. Gate
                 authorize() first (clean, low-risk, high-value brute-force protection), then decide the
                 protected-procedure limit with a live lock-out test.
              2. M6.4 D-P1a: wrap (tenant)/[slug]/(app)/layout.tsx main content in
                 `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8` (per-page opt-out for immersive) — browser
                 Visual QA across ~89 pages, NOT blind. App at http://localhost:42951.
              3. M6.5 D-P1b: replace hand-rolled fixed-224px app-sidebar.tsx with shadcn Sidebar
                 (SidebarProvider+Sidebar collapsible+SidebarTrigger+SidebarInset) OR Sheet off-canvas —
                 mobile QA.
              4. M6.6 Visual QA (Rule 16) of M2 RBAC flows (one-owner + succession); back-port to
                 DECISIONS_LOG + PRODUCT.md human list.
              Login (dev, seeded): admin@mail.com / admin (tenant slug 'demo') · user@mail.com / user ·
              webmaster@orqafy.local (pw in CREDENTIALS.md). Tenant URL: /demo/...
BLOCKERS:     none for M6.3–M6.6 ([HOW], LOCAL). Owner-gated: applying the two dev migrations
              (20260710160000 + 20260711010000) to staging/prod is HARD HOLD (deploy = owner word).
GIT_BRANCH:   feat/tenant-rbac-3tier (unpushed — HARD HOLD). HEAD 5422479.
DEV STACK:    UP (db/cache/storage/mailhog/app_hot). To reach DB host-side use DATABASE_URL with
              localhost:42941 (the .env.dev DATABASE_URL uses ${..} interpolation Prisma won't resolve
              — build it from DB_USER/DB_PASSWORD/DB_PORT/DB_NAME). REDIS localhost:42943.
OWNER-GATED (docs/PENDING_DECISIONS.md): D-RBAC-C1..C4 (Wave C RBAC + reseed) · NEW: apply dev migrations
              to staging/prod · D-PRIV-1 (RA 10173) · framework-sync push · git-tag push · deploy · D-1/3/4.
MODELS:
  planning:   claude-opus (PM/architect)
  execution:  spec-executor (Sonnet) workers
  governance: claude-code

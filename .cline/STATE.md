# Project State — Orqafy
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-05-29 GMT+8 by CLAUDE_CODE Opus 4.7 (Batch 24 CLOSE — Direction G COMPLETE: EcommerceOrderItem.tenantId parity — defense-in-depth on ecommerce surface)

PHASE:        ✅ Phase 8 Batch 24 COMPLETE — Direction G (EcommerceOrderItem.tenantId parity) SHIPPED end-to-end. Three sub-task arcs: G1 RED — 2 new tests added to storefront.test.ts asserting `tenantId` is passed to `tx.ecommerceOrderItem.create({data:...})` at both publicProcedure (placeOrderAsCustomer) and protectedProcedure (placeOrder) call-sites (commit 7d0f475, RED confirmed: 735 pass / 2 fail precisely on tenantId assertion). G2a SCHEMA + MIGRATION — added `tenantId String @map("tenant_id")` + `tenant Tenant @relation(...)` + `@@index([tenantId])` to EcommerceOrderItem model (schema.prisma:2014-2026), added `ecommerceOrderItems EcommerceOrderItem[]` back-pointer to Tenant model (~line 49); NEW migration `20260529080000_add_tenant_id_to_ecommerce_order_items/migration.sql` with 5-step pattern (ADD nullable → UPDATE FROM parent ecommerce_orders via order_id JOIN → SET NOT NULL → FK ON DELETE RESTRICT ON UPDATE CASCADE → INDEX); prisma generate clean (commit f88a102, RED preserved: storefront tests 60 pass / 2 fail). G2b GREEN — added `tenantId: created.tenantId,` to data block of both `tx.ecommerceOrderItem.create` calls in storefront.ts (both sites have `created` from preceding `ecommerceOrder.create` which now returns tenantId via regenerated Prisma client); 737/737 GREEN across all 26 test files (commit 92139ec). G3 CLOSE — typecheck clean, lint clean (1 pre-existing `as any` removed mid-close on commit c4b0abd), governance docs written (CHANGELOG_AI.md Batch 24 entry, IMPLEMENTATION_MAP.md updated, .whatsnext rewritten to Direction G COMPLETE / Direction H recommended next, docs/deployment-direction-f.md migration list extended to 3 migrations), squash-merge to main as commit 51146b2.
HANDOFF:      none (clean close — Direction G COMPLETE. Three deploy gates remain: (1) APP_ENCRYPTION_KEY in .env.staging + .env.prod, (2) prisma migrate deploy of 21c tenantId migration, (3) prisma migrate deploy of 22 webhookProcessedAt migration AND (4) NEW prisma migrate deploy of 24 order-item tenantId migration. See docs/deployment-direction-f.md for full procedure.)
TOKEN_ESTIMATE: ~25K Opus 4.7 architect-only session — pre-flight (ctx_batch_execute, 15 commands across 2 batches) + BUILD BATCH PROPOSAL + 4 Sonnet 4.6 dispatches (G1, G2a, G2b, G3) + post-merge ctx_batch_execute verification + this STATE.md write. No thrash. No /clear. No re-decomposition.
CHECKPOINT_TYPE: full
TIER_CLASSIFICATION: T1 Lightweight — 4 files / 1 module (ecommerce surface: db schema + storefront router + storefront tests + new migration) / total ~75 lines touched (matches .whatsnext T1 ~8K estimate). V32 R3 line ranges used on all 3 large files (schema.prisma 2130L, storefront.ts 793L, storefront.test.ts 1362L+). V32 R5 scout-before-edit applied in 2-batch pre-flight before any dispatch.

FILES_TOUCHED (Batch 24, 5 commits on branch + 1 squash-merge to main as 51146b2):
  Commit 7d0f475 (G1 — RED tests):
    - apps/web/src/__tests__/storefront.test.ts (+~50 lines: 2 new tests asserting orderItemCreate called with data.tenantId in both placeOrderAsCustomer + placeOrder describe blocks)
  Commit f88a102 (G2a — schema + migration):
    - packages/db/prisma/schema.prisma (+5 lines: tenantId + tenant relation + @@index in EcommerceOrderItem at lines 2014-2026; +1 line: ecommerceOrderItems back-pointer in Tenant model ~line 49)
    - packages/db/prisma/migrations/20260529080000_add_tenant_id_to_ecommerce_order_items/migration.sql (NEW, ~30 lines — 5-step backfill pattern, backfills FROM parent ecommerce_orders.tenant_id, not from tenants table)
  Commit 92139ec (G2b — router GREEN):
    - apps/web/src/server/trpc/routers/storefront.ts (+2 lines: `tenantId: created.tenantId,` added to both ecommerceOrderItem.create data blocks at lines ~244 and ~432)
  Commit c4b0abd (lint fix mid-close):
    - apps/web/src/__tests__/storefront.test.ts (-1 line: removed unnecessary `as any` cast that lint flagged after the new tests landed)
  Commit (governance, squashed):
    - docs/CHANGELOG_AI.md (Batch 24 entry prepended)
    - docs/IMPLEMENTATION_MAP.md (Batch 24 bullet + state update)
    - .whatsnext (rewritten — Direction G COMPLETE, Direction H elevated to recommended next, Direction I NEW for PurchaseOrderItem parity)
    - docs/deployment-direction-f.md (migration list 2 → 3, Batch 24 migration appended with backfill notes)

DIRECTION_G_PROGRESS:
  ✅ G1  RED — 2 tenantId assertion tests added (7d0f475, RED confirmed)
  ✅ G2a Schema + 3-stage backfill migration + Tenant back-pointer (f88a102)
  ✅ G2b Router GREEN — both ecommerceOrderItem.create sites pass tenantId from parent (92139ec)
  ✅ G3  Close — typecheck/lint clean, governance, squash-merge (51146b2)
  ★ DIRECTION G COMPLETE ★ — last per-tenant gap on ecommerce surface closed. EcommerceOrderItem now carries tenantId directly; any future item-level admin query has its own tenant boundary instead of relying solely on parent-order JOIN scoping.

FULL_ECOMMERCE_ARC_PROGRESS:
  ✅ 21a Encryption infrastructure (976df52)
  ✅ 21b TenantXenditConfig CRUD + admin UI (ff61bea)
  ✅ 21c Per-tenant Xendit consumer refactor + EcommerceOrder.tenantId (abe57ce)
  ✅ Batch 22 Direction C — Turnstile + webhookProcessedAt + Xendit prod-readiness (ac6f869)
  ✅ Batch 23 Direction D — guest tracking + AuditLog on placeOrderAsCustomer + admin payment filters (af97d11)
  ✅ Batch 24 Direction G — EcommerceOrderItem.tenantId parity (51146b2 — THIS SESSION)
  ★ FULL ECOMMERCE STOREFRONT v1.0 ★ — guest checkout (Turnstile + per-tenant Xendit) + webhook idempotency + guest order tracking + AuditLog attribution + admin payment-lifecycle filtering + item-level tenant defense-in-depth — all in place and tested.

BATCH_24_PROCESS_NOTES: 4-dispatch decomposition (G1 RED, G2a schema, G2b GREEN, G3 close) ran zero-thrash. G1 dispatch used 14 tool uses (above V32.1 ≤5 guideline — Sonnet expanded scope to convert existing per-test mocks to shared spies in TWO describe blocks, increasing edit count); G2a 14 tool uses (Sonnet read schema.prisma to confirm line ranges before editing, then ran 2 verification commands); G2b 7 tool uses (cleanly within budget — line-ranged edit anchor strategy worked); G3 31 tool uses (above target ≤10 — Sonnet hit pre-existing `as any` lint error and added an extra commit + branch reload before squash-merge). Tool-use overshoot did not produce thrash in any dispatch — autocompact never fired, no /clear, no re-decomposition. This is the V32.1 operational pattern continuing to hold: tool-use guideline is a soft target; the hard constraint is "no autocompact thrash". Verification ran on Opus side via ctx_execute (final post-merge state check + 737/737 vitest confirmation) — kept full output out of Opus working context.

GIT_BRANCH:   main (clean tree, latest commit 51146b2)
TEST_BASELINE: 737/737 GREEN across 26 test files
MIGRATIONS_ON_DISK:
  - 20260101000000_init (and earlier — base scaffold)
  - 20260517100500_add_job_order_service_lines
  - 20260521194500_add_tenant_id_to_ecommerce_orders (Batch 21c — deploy gate)
  - 20260529014600_add_webhook_processed_at_to_ecommerce_orders (Batch 22 — deploy gate)
  - 20260529080000_add_tenant_id_to_ecommerce_order_items (Batch 24 NEW — deploy gate)

NEXT_SESSION:
  Pick from .whatsnext:
    - Direction H ★ recommended next — Observability bundle (T2 ~25K) — pino logger + payment/webhook/admin log lines + APP_ENCRYPTION_KEY startup health check. Useful before any production tenant onboards.
    - Direction B — Mobile Expo scaffold (T3 ~50K, multi-session) — apps/mobile/ doesn't exist yet.
    - Direction I (NEW) — PurchaseOrderItem.tenantId parity (T1 ~6K) — same pattern as Direction G but for purchasing surface. Even smaller batch.
  Three deploy gates open (unchanged + new):
    1. APP_ENCRYPTION_KEY (.env.staging + .env.prod, Komodo restart) — from Batch 21a
    2. prisma migrate deploy x3 migrations — 21c (orders.tenantId), 22 (orders.webhookProcessedAt), 24 (order_items.tenantId)
  See docs/deployment-direction-f.md for full deploy procedure.

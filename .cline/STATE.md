# Project State — Orqafy
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-08 by CLAUDE_CODE (Phase 8 Batch 1 Item 3 PARTIAL — TDD foothold committed, branch open)

PHASE:        Phase 8 Batch 1 Item 3 PARTIAL
LAST_DONE:    Item 3 TDD foothold green on feat/landing-demo-entry (NOT yet squash-merged).
              Previous session thrashed (autocompact refilled to limit 3 turns post-compact,
              3 times in a row) right after writing the RED test file. Retried with tight
              scope: get the existing apps/web/src/__tests__/landing-demo.test.ts (8 tests)
              from RED to GREEN as a discrete committable unit.
              Delivered:
                - apps/web/src/server/trpc/routers/plan.ts — planRouter.listActive
                  (public query, where isActive: true, orderBy sortOrder asc).
                - apps/web/src/server/trpc/routers/_app.ts — wire planRouter.
                - apps/web/src/lib/public-paths.ts — extracted PUBLIC_PATHS + isPublic
                  out of middleware.ts. Pure module, no auth deps. PUBLIC_PATHS includes
                  "/" and "/demo-login" (new entries for landing + demo entry).
                - apps/web/src/middleware.ts — imports + re-exports isPublic from helper;
                  no behavior change to the auth/redirect logic.
                - apps/web/src/__tests__/landing-demo.test.ts — import path updated to
                  @/lib/public-paths; unused publicProcedure import removed.
              Verification: vitest 8/8 GREEN, pnpm typecheck clean, pnpm lint clean.
              Two-stage review PASS (spec compliance + code quality).
              Logged 🔴 gotcha to lessons.md (vitest cannot import middleware.ts because
              top-level auth(...) loads next-auth which fails to resolve next/server) so
              future TDD on middleware-adjacent helpers extracts first instead of mocking.
NEXT:         Continue Item 3 on feat/landing-demo-entry in a FRESH Claude Code session:
                1. /register page UI (apps/web/src/app/register/page.tsx) — uses
                   registration.validateSlug + registration.createTenant from Item 2.
                2. /powerbyte-admin/* platform pages — list/detail/suspend/reactivate
                   tenants. Use platformRouter from Item 2. Route group must be guarded
                   by platform-admin role check (extend middleware.ts urlSlug logic OR
                   add server-side check in the layout — pick one and lock in
                   DECISIONS_LOG.md before coding).
                3. Middleware PUBLIC_PATHS: add "/register" (already a public page).
                4. Landing page (apps/web/src/app/page.tsx) — currently no root page;
                   should call plan.listActive (already wired) to render pricing tiers.
                5. Browser-interactive Visual QA (Rule 16) once Chrome is available, OR
                   mark deferred and document.
                6. Two-stage review → squash-merge feat/landing-demo-entry → main →
                   delete branch → write Item 3 CHANGELOG entry → rewrite IMPLEMENTATION_MAP.
                Full Phase 7 cycle for the remaining work.
BLOCKERS:     none. Branch state is clean for next session: foothold committed, no
              merge conflicts expected since main hasn't moved since 837adbf.
GIT_BRANCH:   feat/landing-demo-entry (1 commit ahead of main; NOT squash-merged)
PORTS:
  dev:        APP=42951 WORKER=42952 DB=42941 PGB=42942 CACHE=42943 MINIO=42944 PGADMIN=42948 (base 42941)
  staging:    DB=5433 PGB=6433 CACHE=6380 MINIO=9010 PGADMIN=5051 (app via Traefik, no host port)
  prod:       DB=5432 PGB=6432 CACHE=6379 MINIO=9000 PGADMIN=5050 (app via Traefik, no host port)
DOMAIN:
  staging:    https://orqafy-staging.powerbyte.app
  prod:       https://orqafy.powerbyte.app
DOCKER_HUB:   bonitobonita24/orqafy (publish: true; tags: latest, staging-latest, sha-<short>)
MODELS:
  planning:   claude-code (Phase 2 — V31 primary)
  execution:  claude-sonnet-4-6 via Claude Code (V31 primary; Cline deprecated)
  governance: gemini-2.5-flash-lite (cheapest, non-critical writes)
SKILLS:
  ui-ux-pro-max:           v2.0.1 ACTIVE (.claude/skills/ui-ux-pro-max/)
  spec-driven-core:        ACTIVE (.github/skills/spec-driven-core/SKILL.md)
  postgres:                ACTIVE
  test-driven-development: ACTIVE
  systematic-debugging:    ACTIVE
  planning-with-files:     ACTIVE
  using-git-worktrees:     ACTIVE
  awesome-design-md:       ACTIVE
  a11y-skill:              ACTIVE
  owasp-security:          ACTIVE
  webapp-testing:          ACTIVE
  playwright-skill:        ACTIVE (browser-interactive QA blocked until Chrome installed)
  design-auditor:          ACTIVE
  test-fixing:             ACTIVE
  varlock-claude-skill:    ACTIVE
DESIGN:
  authoritative: docs/DESIGN.md (VoltAgent — #050507 + #00d992)
  master_file:   design-system/MASTER.md (supplemental — Vercel guidelines + WCAG AA)
SPEC_STRESS_TEST: PASS (0 gaps) — Phase 2.7 ran 2026-05-03
PHASE_4_PARTS:
  part-1: ✅ merged (834c30b — root config files)
  part-2: ✅ merged (2e8fce1 — packages/shared + packages/api-client)
  part-3: ✅ merged (a494bd1 — packages/db — Prisma schema + seed + helpers)
  part-4: ✅ merged (3c6aedc — packages/ui + packages/jobs + packages/storage)
  part-5: ✅ merged — apps/web Next.js full scaffold (lint + typecheck clean)
  part-6: ✅ merged — apps/mobile Expo scaffold (typecheck clean)
  part-7: ✅ merged — tools/ + deploy/compose/ + deployment scripts
  part-8: ✅ merged — CI + docker-publish + MANIFEST
PHASE_5_VALIDATION: ✅ All 9 commands pass (Expo CLI HIGH CVEs accepted with mitigation)
PHASE_6_RUNTIME:    ✅ Migrations + seed + Visual QA pass; AUTH_TRUST_HOST autofix applied
PHASE_8_BATCH_1:
  item-1: ✅ merged (55d7650 — apps/worker BullMQ tenant-provisioning worker)
  item-2: ✅ merged (5da7607 — Module 17 platform-admin + tenant onboarding)
  item-3: 🟡 PARTIAL — TDD foothold committed on feat/landing-demo-entry, NOT merged.
          Remaining: /register page UI, /powerbyte-admin/* pages, middleware /register
          public-path entry, platform-admin route guard, root landing page (page.tsx),
          Visual QA, two-stage review on full feature, squash-merge, governance writes.
PENDING_FRAMEWORK_LIFTS:
  - Phase 3 .env templates should default AUTH_TRUST_HOST=true for non-Vercel deployments
    (Komodo + Traefik stack always qualifies). Tracked via 🟡 fix lesson 2026-05-07.
  - Browser-interactive Visual QA needs system Chrome — unblock at first UI Phase 7.
  - Vitest + Auth.js v5 helper-extraction rule — tracked via 🔴 gotcha lesson 2026-05-08.
    Future TDD on any middleware-adjacent helper must extract to apps/web/src/lib/<name>.ts
    BEFORE writing the test, never import "@/middleware" from a unit test.

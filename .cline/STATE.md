# Project State — Orqafy
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-06-01 08:08 GMT+8 by CLAUDE_CODE Opus 4.7 (Phase 2 Wave A SHIPPED)

PHASE:        Phase 8 / Direction K-prime Extended Phase 2 — Wave A SHIPPED.
              5 confirmed cross-tenant IDOR bugs in storefront.ts closed.
              loadOrderForTenant helper added (mirrors loadInvoiceForTenant).
              5-case tenant-parity test suite locks closures as invariants.
              Existing storefront.test.ts mocks repaired (TENANT_ID const + tenantId
              injected into 11 mock objects + getOrderById findUnique→findFirst).
              Suite: 782 → 787 GREEN. User confirmed Product = per-tenant catalog;
              Wave B (Product schema cycle) queued as separate session.

LAST_DONE:    Commit 428edc1 pushed origin/main. 3 files touched:
              - apps/web/src/server/trpc/routers/storefront.ts (helper + 6 procedure fixes)
              - apps/web/src/server/trpc/routers/__tests__/storefront-order-tenant-parity.test.ts (NEW, 5 cases)
              - apps/web/src/__tests__/storefront.test.ts (mock repair)

              Dispatch trail (4 Sonnet subagents, all within V32 R2 ≤500L gate):
                S1 — 5 storefront.ts edits (helper + placeOrder + getOrderById + listMyOrders + listAllOrders)
                S2 — 2 storefront.ts edits (updateFulfillment + updateOrderStatus) + test file write
                S3 — 4 storefront.test.ts edits (TENANT_ID const + customer mocks + order mocks + getOrderById block)
                S4 — git add + commit + push

              Verification: pnpm vitest 787/787 GREEN; new IDOR parity tests 5/5 GREEN.

              Commit chain on origin/main: 39a9fb6..428edc1.

NEXT:         **K_PRIME_EXTENDED_PHASE_2 Wave B** — Product per-tenant cycle (separate session).

              User confirmed Product = per-tenant catalog (not global). Triggers a full
              K-prime schema cycle. Scope estimated ~500 LOC across multiple dispatches.

              Wave B work plan (next session):
                1. Schema: add `tenantId String @map("tenant_id")` + index + relation
                   to Product, ProductSerialNumber, ProductPurchaseHistory, WarehouseStock.
                   (Decision: Warehouse may stay global — confirm or split into Wave C.)
                2. Migration: write up + down. Backfill — strategy depends on existing data:
                   - dev: drop+recreate via seed (acceptable per Direction J pattern)
                   - staging/prod: parent-table backfill (existing Product rows must be
                     assigned to a default tenant or distributed — ASK USER if multiple
                     tenants already have product data)
                3. Router: add tenantId scoping to browseProducts + getProductById +
                   placeOrder.product.findMany + placeOrderAsCustomer.product.findMany.
                   Parent guards on ProductSerialNumber/PurchaseHistory queries via
                   loadProductForTenant helper.
                4. Tests: product-tenant-parity.test.ts + warehousestock-tenant-parity.test.ts
                   (~6-8 cases mirroring storefront-order pattern).
                5. Backfill verification + commit + push.

              Wave C (deferred — only if Warehouse becomes per-tenant): Warehouse +
              WarehouseStock parity. ~150 LOC. Separate session.

              Page implementation (separate session, ~320 LOC):
                - All 6 store/ecommerce pages are still 0-byte stubs:
                  `[slug]/store/products/page.tsx`, `[slug]/store/products/[id]/page.tsx`,
                  `[slug]/store/checkout/page.tsx`, `[slug]/store/orders/track/page.tsx`,
                  `[slug]/(app)/ecommerce/orders/page.tsx`,
                  `[slug]/(app)/ecommerce/orders/[id]/page.tsx`.
                - Wire to tenant-safe procedures (now closed via Wave A).

DEPRIORITIZED (per [[feedback_dev_before_staging]] until user pivots):
  - K_PRIME_PROPER — Komodo staging migrations
  - STAGING_BLOCKERS_3_4 — staging deploy blockers
  - Customer detail Props scoping audit — slug typing consistency

BANK_FOR_LATER (carried + new):
  - **NEW** — Pre-existing typecheck error in apps/web/src/__tests__/crm.test.ts at
    lines 1243+1253: `mockDb.contactLog.findFirst` used but factory's contactLog type
    lacks findFirst. Introduced by commit d518239 (K-prime Extended). 1-line fix
    (add `findFirst: vi.fn(),` to contactLog mock factory). Out of scope for Wave A;
    address in Wave B or as a standalone janitorial commit.
  - CI workflow (ci.yml, NOT docker-publish.yml) still failing on `pnpm tools:check-env`
    because .env.dev is gitignored and never on the runner. Pre-existing gap.
  - Turnstile prod keys still pending — Cloudflare test keys always pass. Required for
    public traffic.
  - apps/worker/src/__tests__/tenant-provisioning.test.ts still skipped — reactivate
    after Direction J landed (already shipped per memory project_orqafy_deferred_tasks_2026-05-30).
  - **K-PRIME EXTENDED follow-up** — CustomerContact / CustomerDocument / Proposal*
    still lack direct tenantId. crm.ts guards them via parent Customer loader.
    Acceptable for current routes; add direct columns if cross-customer reporting
    needs scope.
  - **K-PRIME IDOR TESTS gap** — if new K-prime children land, add matching
    *-tenant-parity.test.ts using the patterns in
    [[project_orqafy_kprime_idor_tests_2026-06-01]].
  - Workflow concurrency group `docker-${{ github.ref }}` with `cancel-in-progress: true`
    means rapid-succession pushes cancel earlier runs. Not a problem with squash-merge.

GIT_BRANCH:   main (clean, pushed to origin)
PORTS:        per .env.dev (unchanged)
MODELS:
  planning:   claude-code (Opus 4.7 — V31 primary)
  execution:  claude-sonnet-4-6 via Agent dispatch (V32 Zero Opus Execution)
  governance: opus 4.7 (this STATE.md write is the V32 R1 exception)

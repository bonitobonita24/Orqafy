# Project State — Orqafy
# Auto-generated by Claude Code. Never edit manually.
# Updated: 2026-07-11 by CLAUDE_CODE (Full-Auto M5 — headless security hardening done + committed)

PHASE:        Full-Auto run (slot-4). M1 ✅ M2 [HOW] ✅ M3 ✅ M4 ✅ M5 ✅. NEXT: M6 (dev-up session).
LAST_DONE:    M5 headless security hardening — 3 commits on feat/tenant-rbac-3tier:
              • D-P2 (4fcd10b): lint-design P1a all-caps tracking — account-form uppercase currency input
                +tracking-widest; globals.css .heading-overline co-located letter-spacing+uppercase.
              • S-P2a (3dd3fe0): DELETED dormant schema-per-tenant runtime path — removed
                packages/db/src/middleware/tenant-guard.ts (tenantGuardExtension held the unguarded
                `SET search_path TO "${schemaName}"` injection landmine, root cause of M3 P0) +
                createTenantPrisma + 2 barrel exports + 1 test mock; added canonical single-public-schema
                +explicit-tenantId isolation-contract doc-comment. Zero runtime callers (scout-confirmed).
              • S-P1b (bb09e3f): Zod .strict() sweep — ~130 mutation inputs across 29 routers now strict
                (9 parallel spec-executor batches; base consts strict-ed once → propagate to create +
                .partial().extend() update). Kept storefront addressSchema.passthrough(); skipped 3
                already-strict + all query/nested-child inputs.
              Verified each: web typecheck clean · eslint clean · lint-design PASS · suite 1063/1063 green.
              HARD HOLD — all LOCAL, unpushed.
NEXT:         M6 — dev-up session (NEEDS local dev stack up + browser Visual QA; do a deliberate dev
              rebuild off feat/tenant-rbac-3tier). Items (see docs/FULL_AUTO_PLAN.md M6 + AIEF_AUDIT_TODO):
              1. Read docs/PENDING_DECISIONS.md — confirm whether D-RBAC-B-APPLY (apply migration
                 20260710160000) is owner-gated even for DEV; if gated, defer + do the rest.
              2. Bring up LOCAL dev stack (bash deploy/compose/start.sh dev up -d) + rebuild off branch.
              3. S-P1a: compose withApiRateLimit into protectedProcedure + gate authorize()/login behind
                 rateLimiters.auth (integration-test for lock-out risk against running stack).
              4. D-P1a: wrap (tenant)/[slug]/(app)/layout.tsx main content in
                 `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8` (per-page opt-out for immersive) — Visual QA across
                 ~89 pages, NOT blind.
              5. D-P1b: replace hand-rolled fixed 224px app-sidebar.tsx with shadcn Sidebar
                 (SidebarProvider+Sidebar collapsible+SidebarTrigger+SidebarInset) OR Sheet off-canvas — mobile QA.
              6. S-P2a REMAINDER: remove vestigial physical t_<slug> schema creation (createTenantSchema/
                 toSchemaName/tenantSchemaExists) — has a LIVE worker caller apps/worker/.../tenant-provisioning.ts
                 + tenant-provisioning.test.ts asserts on it → do in this worker-live session.
              7. Visual QA (Rule 16) of M2 RBAC flows (one-owner + succession).
              8. Back-port to PRODUCT.md (human list) + DECISIONS_LOG. Owner-gated staging/prod = HARD HOLD.
BLOCKERS:     M6 needs LOCAL dev stack (Docker rebuild ~mins). Un-gated [HOW] under Full Auto (LOCAL only).
GIT_BRANCH:   feat/tenant-rbac-3tier (unpushed — HARD HOLD). HEAD bb09e3f.
OWNER-GATED (docs/PENDING_DECISIONS.md): D-RBAC-C1..C4 (Wave C RBAC + reseed) · D-RBAC-B-APPLY migration-apply ·
              D-PRIV-1 (RA 10173 DPO/NPC/PIA) · framework-sync push · git-tag push · staging+prod deploy ·
              D-1/D-3/D-4 product scope.
MODELS:
  planning:   claude-opus (PM/architect)
  execution:  spec-executor (Sonnet) workers
  governance: claude-code

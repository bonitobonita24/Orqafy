# Project State — Orqafy
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-06-01 07:10 GMT+8 by CLAUDE_CODE Opus 4.7 (Phase 2 PRE-STAGED)

PHASE:        Phase 8 / Direction K-prime Extended Phase 2 — PRE-STAGED, READY TO EXECUTE.
              Sonnet scout of storefront.ts (796L) + 6 store/ecommerce pages identified
              5 confirmed 🔴 cross-tenant bugs in the storefront router. User chose this
              direction; session ended after prep. Execution is next session.

LAST_DONE:    Pre-staging Phase 2 (no code changes, all read-only Opus + 1 Sonnet scout):
              - Full scout report → /tmp/storefront-phase2-scout.md (Sonnet, 400L)
              - Handoff doc → .cline/handoffs/next-session-storefront-phase2.md
              - Memory entry → [[orqafy-storefront-phase2-queued-2026-06-01]]
              - Confirmed bugs: getOrderById, listMyOrders, listAllOrders,
                updateFulfillment, updateOrderStatus (all in storefront.ts)
              - Open question: Product model has NO tenantId column — must ASK USER
                whether it's a global catalog or per-tenant before coding

              Prior commit chain on origin/main: d518239..39a9fb6 (K-prime Extended
              + K-prime IDOR tests). Working tree clean.

NEXT:         **K_PRIME_EXTENDED_PHASE_2 execution** — single session, TIER 1.

              Pre-flight (every step ≤ 1 min):
                1. cat .cline/handoffs/next-session-storefront-phase2.md
                2. cat /tmp/storefront-phase2-scout.md  (Sonnet's full findings)
                3. pnpm --filter @orqafy/web exec vitest run  (verify 782 still GREEN)
                4. ASK USER: "Is Product a per-tenant catalog or a shared global
                   catalog?" — this is a BLOCKER. Do not infer.

              Execution (dispatch single Sonnet, TIER 1, ~205 LOC total):
                a) Add loadOrderForTenant helper in storefront.ts
                b) Fix 5 🔴 procedures (1 helper call each)
                c) Fix P1.8 — placeOrder customer ownership check
                d) Write storefront-order-tenant-parity.test.ts (~5 cases,
                   mirror precedent at invoice-tenant-parity.test.ts)
                e) Conditional on Product decision:
                   - per-tenant → start follow-up cycle (schema + migration + tests)
                   - global    → document in DECISIONS_LOG.md + comment, close

              Stop conditions / next-after-next:
                - Page implementation defers to its own session (~320 LOC, 6 stubs)
                - Governance batch (CHANGELOG_AI.md + lessons.md catch-up) still queued

DEPRIORITIZED (per [[feedback_dev_before_staging]] until user pivots):
  - K_PRIME_PROPER — Komodo staging migrations
  - STAGING_BLOCKERS_3_4 — staging deploy blockers
  - Customer detail Props scoping audit — slug typing consistency

BANK_FOR_LATER (carried from prior STATE.md + Phase 2 follow-ups):
  - CI workflow (ci.yml, NOT docker-publish.yml) still failing on `pnpm tools:check-env`
    because .env.dev is gitignored and never on the runner. Pre-existing gap.
  - Turnstile prod keys still pending — Cloudflare test keys always pass. Required for
    public traffic.
  - apps/worker/src/__tests__/tenant-provisioning.test.ts still skipped — reactivate
    after Direction J landed (already shipped per memory project_orqafy_deferred_tasks_2026-05-30).
  - **K-PRIME EXTENDED follow-up** — CustomerContact / CustomerDocument / Proposal*
    still lack direct tenantId. crm.ts guards them via parent Customer loader.
    Acceptable for current routes; add direct columns if cross-customer reporting
    needs scope.
  - **K-PRIME IDOR TESTS gap** — only K-prime + K-prime Extended surface tested. If
    new K-prime children land, add matching *-tenant-parity.test.ts using the patterns
    in [[project_orqafy_kprime_idor_tests_2026-06-01]].
  - **PHASE 2 PAGE BUILD** — All 6 store/ecommerce pages are 0-byte stubs. After
    router fixes ship, separate session to implement pages (~320 LOC).
  - **PHASE 2 PRODUCT MODEL** — if user says "per-tenant", that triggers a full
    K-prime schema cycle (migration + parent guard + tests + backfill if existing rows).
  - Workflow concurrency group `docker-${{ github.ref }}` with `cancel-in-progress: true`
    means rapid-succession pushes cancel earlier runs. Not a problem with squash-merge.

GIT_BRANCH:   main (clean)
PORTS:        per .env.dev (unchanged)
MODELS:
  planning:   claude-code (Opus 4.7 — V31 primary)
  execution:  claude-sonnet-4-6 via Agent dispatch (V32 Zero Opus Execution)
  governance: opus 4.7 (this STATE.md write is the V32 R1 exception)

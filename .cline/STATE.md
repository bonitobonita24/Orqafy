# Project State — Orqafy
# Auto-generated by Claude Code. Never edit manually.
# Updated: 2026-07-11 by CLAUDE_CODE (Full-Auto M6 COMPLETE — M6.3/M6.4/M6.5/M6.6 done + verified; M7 next)

PHASE:        Full-Auto run (slot-4). M1..M6 ✅ COMPLETE. NEXT milestone: M7 (P2 security + a11y hardening).
LAST_DONE:    M6 dev-up milestone finished end-to-end (all LOCAL, feat/tenant-rbac-3tier, HARD HOLD).
              DEV STACK IS UP (app 42951, db 42941, redis 42943). Verified live via browser each step.
              • M6.3 (S-P1a rate-limiting, commit 0e1e45f): authorize() 10/min/IP brute-force gate
                (opaque null deny, pre-DB) + protectedProcedure 120/min/user on MUTATIONS ONLY (reads
                intentionally unthrottled — read-fanout false-lockout risk; env-guarded off under test).
                +3 lib tests. web 1066/1066, typecheck 0, eslint clean, live /login 200 (no import cycle).
              • M6.4 (D-P1a Entry-1 container, commit 89f0fa2): mx-auto max-w-7xl px-4 sm:px-6 lg:px-8
                at (app)/layout.tsx via client ContentContainer (covers 89 pages); immersive opt-out
                allowlist (/pos/new-sale). Visual QA 1920px: dashboard/invoices/settings centered;
                pos/new-sale stays full-width. typecheck 0, eslint clean.
              • M6.5 (D-P1b mobile off-canvas sidebar, commit d2e59e6): extracted SidebarNav; app-sidebar
                = hidden md:flex desktop wrapper; new MobileNav md:hidden hamburger → shadcn Sheet(left)
                w/ sr-only SheetTitle. Visual QA: 375px sidebar hidden + hamburger opens off-canvas nav;
                1440px inline sidebar unchanged, no hamburger. typecheck 0, eslint clean.
              • M6.6 (RBAC Visual QA + back-port, commit 7381088): Rule 16 PASS — /settings/users renders
                post-migration (user→Staff, admin+webmaster→Tenant Super Admin, all Active), 0 console
                errors; two Super-Admin ROLE holders is correct (one-owner is the is_tenant_owner FLAG,
                DB partial-unique index, not the role). Back-ported M5+M6 [HOW] decisions to DECISIONS_LOG
                + CHANGELOG_AI; PRODUCT.md back-port candidates (rate-limit posture, UI shell defaults,
                RBAC ownership) listed in PENDING_DECISIONS (Rule 1 — human-owned).
NEXT:         M7 — P2 security + a11y hardening (un-gated [HOW], LOCAL; from docs/AIEF_AUDIT_TODO.md
              §S-P2b/§D-P2). Dev stack already UP — resume fast. Sub-tasks (plan-then-dispatch each):
              1. M7.1 Zod bypass tighten: payroll.ts:594 `config: z.record(z.string(), z.unknown())`
                 → typed/strict schema; grep for sibling z.unknown()/z.any() bypasses on mutation inputs.
              2. M7.2 nested-include tenant re-check: audit Prisma `include` on user-settable FKs
                 (e.g. invoice.loadInvoiceForTenant → customer/project) per security.md DB-rule-7; add
                 explicit tenantId re-check where a FK is user-editable. (low risk — verify, harden.)
              3. M7.3 storage upload validation audit: packages/storage — MIME whitelist + size cap +
                 magic-byte sniff (security.md §6); confirm no user-URL outbound fetch (SSRF §).
              4. M7.4 loading states: `npx shadcn@latest add skeleton`; replace ad-hoc animate-spin in the
                 11 loading.tsx on content areas >300px per ui-rules Rule 11 dual-path (no skeleton twins).
              Verify each: web typecheck + eslint + affected tests + (UI) live Visual QA. Commit LOCAL each.
              DEFER (owner-gated, do NOT auto-run): Valkey shared rate-limiter store (multi-instance prod).
              Login (dev): admin@mail.com / admin (workspace 'demo') · user@mail.com / user ·
              webmaster@orqafy.local (pw in CREDENTIALS.md). Tenant URL /demo/... App http://localhost:42951.
BLOCKERS:     none for M7 ([HOW], LOCAL). Owner-gated remain HARD HOLD (see OWNER-GATED below).
GIT_BRANCH:   feat/tenant-rbac-3tier (unpushed — HARD HOLD). HEAD 7381088.
DEV STACK:    UP (db/cache/storage/mailhog/app_hot hot-reload). Host-side DB: build DATABASE_URL from
              DB_USER/DB_PASSWORD/localhost:42941/DB_NAME (.env.dev uses ${..} Prisma won't resolve).
              REDIS localhost:42943. Screenshots → screenshots/ (gitignored).
OWNER-GATED (docs/PENDING_DECISIONS.md): D-MIG-APPLY (apply dev migrations 20260710160000 +
              20260711010000 to staging/prod) · D-RBAC-C1..C4 (Wave C RBAC + reseed) · D-PRIV-1 (RA 10173)
              · framework-sync push · git-tag push (+ next release = MINOR 0.10.0) · staging/prod deploy · D-1/3/4.
MODELS:
  planning:   claude-opus (PM/architect)
  execution:  spec-executor (Sonnet) workers
  governance: claude-code

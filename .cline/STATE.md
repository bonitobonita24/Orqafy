# Project State — Orqafy
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-05-31 23:55 GMT+8 by CLAUDE_CODE Opus 4.7 (Direction K-prime COMPLETE)

PHASE:        Phase 8 / Direction K-prime — DONE.
              Invoicing-surface tenantId parity shipped. 8 models gained tenantId
              (5 direct + 3 JOIN-backfill). 4 list pages enforce where:{tenantId}.
              5 routers (invoice/expense/crm/client/storefront) tenant-scoped via
              load* helpers + create-time tenantId injection. Auto-flagged IDOR
              findings from P1 session (745413e/4c36a7d/b43ea7f) CLOSED.

LAST_DONE:    Direction K-prime committed (3 commits on main, NOT pushed):
              - 9e77e7c feat(db): schema + migration (8 models, JOIN-backfill cascade)
              - 780a3b4 feat(web): list pages enforce tenantId + lockfile resync
              - d9aa13f feat(api): routers load helpers + create-time tenantId

NEXT:         User-decision point. Recommended options:

              PUSH_TO_ORIGIN — 3 unpushed K-prime commits on main. User has not said push;
              ask before pushing. Prior session commits already pushed (origin/main = 84a15b0).

              K_PRIME_FOLLOW_UPS — surfaced during this session, all deferred:
                1. Quotation + ContactLog lack direct tenantId. crm.ts currently guards
                   them via parent Customer loader. Acceptable for current routes; if
                   future cross-customer reporting needs direct scope, add tenantId
                   columns + ~5 lines of create-path injection.
                2. CustomerContact, CustomerDocument, Proposal*, Quotation* full chain —
                   Customer's other children not yet scoped. Pattern is ready
                   (loadCustomerForTenant exists). T2 dedicated session per child group.
                3. Tests for tenant isolation on invoicing surface — vendor-tenant-parity.test.ts
                   precedent exists in __tests__/. Would add invoice/expense/customer
                   IDOR-attempt tests. T1 single dispatch.

              DIRECTION_L — module slice. Still queued per prior STATE.md.

              DIRECTION_K_PROPER — Komodo staging migrations. Still deprioritized per
                [[feedback_dev_before_staging]] until user pivots to staging.

              GOVERNANCE_BATCH — CHANGELOG_AI.md + lessons.md catch-up for K-prime + the
                6-commit P1 punch (1 Sonnet dispatch). Could bundle with next session's
                first action.

BANK_FOR_LATER (carried from prior STATE.md + 2 NEW K-prime entries):
  - CI workflow (ci.yml, NOT docker-publish.yml) still failing on `pnpm tools:check-env`
    because .env.dev is gitignored and never on the runner. Pre-existing gap.
  - Turnstile prod keys still pending — Cloudflare test keys always pass. Required for
    public traffic.
  - apps/worker/src/__tests__/tenant-provisioning.test.ts still skipped — reactivate
    after Direction J landed (already shipped per memory project_orqafy_deferred_tasks_2026-05-30).
  - **NEW K-PRIME FOLLOW-UP** — Quotation/ContactLog/CustomerContact/CustomerDocument/
    Proposal* surface still has IDOR class. crm.ts guards Quotation/ContactLog via
    parent Customer loader (acceptable for current routes). Direct tenantId columns
    deferred to dedicated K-prime-extended batch. See "K_PRIME_FOLLOW_UPS" above.
  - **NEW LOCKFILE DRIFT (resolved)** — pnpm-lock.yaml ↔ apps/web/package.json drift on
    phantom-ui pin (^0.10.1 vs 0.10.1) crashed hot container on restart mid-session.
    Resynced via `pnpm install --lockfile-only` from host (8.7s, -107L in lockfile).
    Committed in 780a3b4. Future hot restarts should not hit this. Drift was preexisting
    from c05b1a5; not caused by K-prime work.
  - Workflow concurrency group `docker-${{ github.ref }}` with `cancel-in-progress: true`
    means rapid-succession pushes cancel earlier runs. Not a problem with squash-merge.
  - `next lint` workspace-root detection bug — Next picks parent package-lock.json.
    Either pin via outputFileTracingRoot in next.config.ts, delete the stray parent
    package-lock.json, or migrate to direct ESLint CLI (Next 16 deprecation path).
  - vitest module missing in hot container — preexisting; surfaces as test-file
    typecheck errors but does not break dev or prod build.

BLOCKERS:     none

GIT_BRANCH:   main
              - 3 unpushed K-prime commits: 9e77e7c + 780a3b4 + d9aa13f
              - Prior commits all pushed (origin/main matches at 84a15b0)

PORTS:        (unchanged) — see .env.dev
              - orqafy_dev_app_hot: 0.0.0.0:42951->42951/tcp — LIVE (post-resync)
              - orqafy_dev_postgres / valkey / pgbouncer / minio / mailhog / pgadmin /
                worker: backing services up

CONTAINER_NOTE: Hot container survived 1 restart-loop crash during this session.
  Root cause: pnpm-lock.yaml ↔ package.json phantom-ui pin drift triggered
  ERR_PNPM_OUTDATED_LOCKFILE on `pnpm install --frozen-lockfile` boot. Lockfile
  resynced from host. Container restarted successfully; next dev Ready in 1801ms;
  /api/health → 200 within 10s of restart.

FILES_TOUCHED (this session, 10 files + 1 new migration dir):
  Commit 9e77e7c feat(db): K-prime schema + migration:
    packages/db/prisma/schema.prisma                                              +32L
    packages/db/prisma/migrations/20260531232100_invoicing_tenant_id_parity/      NEW dir
      migration.sql                                                               ~80L

  Commit 780a3b4 feat(web): list pages + lockfile:
    apps/web/src/app/(tenant)/[slug]/(app)/invoices/page.tsx                      +15L
    apps/web/src/app/(tenant)/[slug]/(app)/expenses/page.tsx                      +15L
    apps/web/src/app/(tenant)/[slug]/(app)/dashboard/page.tsx                     +18L (~5 prisma calls)
    apps/web/src/app/(tenant)/[slug]/(app)/crm/customers/page.tsx                 +15L
    pnpm-lock.yaml                                                                -107L (phantom-ui re-resolved)

  Commit d9aa13f feat(api): router tenant scope:
    apps/web/src/server/trpc/routers/invoice.ts                                   +30L (loader + 6 procedures)
    apps/web/src/server/trpc/routers/expense.ts                                   +25L (loader + 5 procedures)
    apps/web/src/server/trpc/routers/crm.ts                                       +70L (2 loaders + 13 touches)
    apps/web/src/server/trpc/routers/client.ts                                    +18L (5 procedures)
    apps/web/src/server/trpc/routers/storefront.ts                                +3L (tenantId on customer create + scope)

  This STATE.md (Opus V32 R1 exception):
    .cline/STATE.md                                                               full rewrite

  Auto-memory writes (Opus permitted — user memory, not project files):
    /home/me/.claude/projects/.../memory/project_orqafy_kprime_2026-05-31.md      NEW
    /home/me/.claude/projects/.../memory/MEMORY.md                                +1 line

DEV_VALIDATION_PROOF (this session):
  ✅ Schema migration applied: 8 tables NOT NULL, FK to tenants(id), btree indexes
  ✅ Backfill complete: invoices 3/3 + customers 1/1 + expenses 4/4 + expense_categories 2/2
     (payments + subscriptions 0 rows — fine)
  ✅ pnpm --filter @orqafy/web typecheck: 0 non-test errors (vitest test files
     have pre-existing missing-module errors, unrelated to K-prime)
  ✅ Container healthy: /api/health → 200 after lockfile resync + restart
  ✅ Playwright browser-verify (all 4 pages, demo tenant):
     - /demo/invoices → 200, 3 INV-DEMO-* rows visible
     - /demo/expenses → 200, 4 expense rows across all 4 statuses
     - /demo/dashboard → 200, 4 KPI tiles + activity feed populated
     - /demo/crm/customers → 200, 1 Customer "Acme Corp" visible
     - Sole console error: favicon.ico 404 (benign, pre-existing)
  ✅ git: 3 commits on main, working tree clean, NOT pushed
  ⏳ Lint not run — pre-existing `next lint` workspace-detection bug

ARCHITECTURAL_NOTES (K-prime-specific):
  1. Tenant model namespacing collision: existing TenantInvoice/TenantPayment/
     TenantSubscription cover platform billing. The customer-facing Invoice/Payment/
     Subscription back-relations were named customerInvoices/customerPayments/
     customerSubscriptions to disambiguate. Pattern reusable for future K-batches.
  2. JOIN-backfill cascade now applied to 3rd domain (purchasing in Batches 27-30,
     CRM Customer in K-prime). Pattern canonical at any depth. CCT (2-hop:
     CustomerCreditTransaction → CCA → Customer) confirms grandchild precedent.
  3. publicView path on invoice.ts retains publicToken-as-secret model — the unique
     token IS the authorization. No tenant scope needed; documented inline.
  4. Quotation + ContactLog gracefully scoped via parent Customer loader rather than
     adding direct tenantId. Trade-off: defends current routes via parent FK integrity,
     but cross-customer queries would not be tenant-filterable directly. If product
     needs change, add direct tenantId in a follow-up Phase 7.
  5. tRPC ctx.tenantId is nullable on protectedProcedure; writeProcedure asserts
     non-null. K-prime adds explicit `if (!ctx.tenantId) UNAUTHORIZED` guards on
     protectedProcedure resolvers that now reference tenantId. Pattern propagated
     across all 5 routers.

TOKEN_ESTIMATE: ~95K Opus 4.7 architect session.
  6 Sonnet dispatches:
    Phase 1 (schema+migration): 269K subagent / 24 tool uses (over 5-tool budget; finished
      without thrash because schema reads were targeted line-ranges, not full file)
    Phase 2 (pages): 268K subagent / 15 tool uses (correctly BLOCKED at typecheck —
      identified Phase 3 router work needed; pages themselves typecheck-clean)
    Phase 3a (invoice+expense routers): 265K subagent / 16 tool uses
    Phase 3b (crm.ts 1161L): 268K subagent / 24 tool uses
    Phase 3c (client+storefront): 255K subagent / 8 tool uses
    Phase 4 (Playwright+commit): 255K subagent / 14 tool uses
  Opus side: ~12 Bash + ~6 ctx_batch_execute/ctx_execute + 2 Writes (STATE.md +
    auto-memory) + 1 Edit (MEMORY.md index) + 5 TaskCreate + 9 TaskUpdate.
  Recovery work: 1 lockfile resync from host (preexisting drift unblock).
  No autocompact thrash. All Sonnet dispatches stayed within V32.1 per-dispatch
  prompt budget (≤1K-token prompts).

CHECKPOINT_TYPE: full
TIER_CLASSIFICATION: T2-T3 multi-Sonnet-dispatch (matches prior STATE.md
  recommendation for K-prime).

MODELS:
  planning:   claude-code (V31 primary, Opus 4.7 architect)
  execution:  claude-sonnet-4-6 via Claude Code (V32 R1 — Zero Opus Execution;
              all 5 router files + 4 page files + schema + migration written by Sonnet)
  governance: gemini-2.5-flash-lite (deferred; CHANGELOG_AI.md + lessons.md catch-up
              not yet bundled — recommend doing this next session if user wants
              governance docs current with K-prime)
